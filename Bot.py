import logging
import base64
import os
import sqlite3
import re
import requests
import threading
import time
import sys
from datetime import datetime
from telegram import Update, ReplyKeyboardMarkup, KeyboardButton, ReplyKeyboardRemove
from telegram.ext import Application, CommandHandler, MessageHandler, CallbackQueryHandler, filters, ContextTypes, ConversationHandler
from flask import Flask

# --- الإعدادات ---
TOKEN = os.environ.get('TOKEN', "7324911542:AAGcVkwzjtf3wDB3u7cprOLVyoMLA5JCm8U")
GROQ_KEY = os.environ.get('GROQ_KEY', "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")
GROQ_URL = "https://api.groq.com/openai/v1/chat/completions"
GROQ_MODEL = "meta-llama/llama-4-maverick-17b-128e-instruct"
DB_NAME = "abood-gpt.db"

CANDLE_SPEEDS = ["S5", "S10", "S15", "S30", "M1", "M2", "M3", "M5", "M10", "M15", "M30", "H1", "H4", "D1"]
TRADE_TIMES = ["قصير (1m-15m)", "متوسط (4h-Daily)", "طويل (Weekly-Monthly)"]

# توزيع العملات للنظام الجديد
CATEGORIES = {
    "أزواج العملات 🏛️": [
        "EUR/USD (OTC)", "GBP/USD (OTC)", "USD/JPY (OTC)", "USD/CHF (OTC)",
        "AUD/USD (OTC)", "USD/CAD (OTC)", "NZ$/USD (OTC)", "EUR/GBP (OTC)",
        "EUR/JPY (OTC)", "GBP/JPY (OTC)", "EUR/CHF (OTC)", "AUD/JPY (OTC)",
        "EUR/AUD (OTC)", "EUR/CAD (OTC)", "GBP/AUD (OTC)", "CAD/JPY (OTC)",
        "CHF/JPY (OTC)", "NZD/JPY (OTC)", "GBP/CHF (OTC)", "AUD/CAD (OTC)"
    ],
    "مؤشرات الأسواق 📊": [
        "S&P 500 (OTC)", "Dow Jones (OTC)", "Nasdaq 100 (OTC)", 
        "DAX 40 (OTC)", "CAC 40 (OTC)", "FTSE 100 (OTC)", 
        "Hang Seng (OTC)", "Nikkei 225 (OTC)"
    ],
    "سلع وطاقة 🕯️": [
        "Gold (OTC)", "Silver (OTC)", "UKOIL (OTC)", 
        "USOIL (OTC)", "Natural Gas (OTC)"
    ],
    "أسهم الشركات 🍎": [
        "Apple (OTC)", "Amazon (OTC)", "Google (OTC)", "Facebook (OTC)",
        "Microsoft (OTC)", "Tesla (OTC)", "Netflix (OTC)", "Intel (OTC)",
        "Boeing (OTC)", "Visa (OTC)", "McDonald's (OTC)", "Pfizer (OTC)",
        "Coca-Cola (OTC)", "Disney (OTC)", "Alibaba (OTC)", "Walmart (OTC)"
    ]
}
# حالات المحادثة
MAIN_MENU, SETTINGS_CANDLE, SETTINGS_TIME, CHAT_MODE, ANALYZE_MODE, RECOMMENDATION_MODE, CATEGORY_SELECTION = range(7)

# --- Flask Server ---
app = Flask(__name__)

@app.route('/')
def home():
    return """
    <!DOCTYPE html>
    <html>
    <head>
        <title>Obeida Trading</title>
        <style>
            body { font-family: Arial, sans-serif; text-align: center; padding: 50px; }
            h1 { color: #2c3e50; }
            .status { background: #2ecc71; color: white; padding: 10px 20px; border-radius: 5px; display: inline-block; }
        </style>
    </head>
    <body>
        <h1> 📊 Obeida Trading Telegram Bot 📊</h1>
        <p>Chat & Technical Analysis Bot</p>
        <div class="status">✅ Obeida Trading Running</div>
        <p>Last Ping: """ + time.strftime("%Y-%m-%d %H:%M:%S") + """</p>
    </body>
    </html>
    """

@app.route('/health')
def health():
    return {"status": "active", "timestamp": time.time()}

@app.route('/ping')
def ping():
    return "PONG"

# --- قاعدة البيانات ---
def init_db():
    conn = sqlite3.connect(DB_NAME)
    cursor = conn.cursor()
    cursor.execute('''
        CREATE TABLE IF NOT EXISTS users (
            user_id INTEGER PRIMARY KEY, 
            candle TEXT DEFAULT 'M1', 
            trade_time TEXT DEFAULT 'قصير (1m-15m)',
            chat_context TEXT DEFAULT ''
        )
    ''')
    cursor.execute('''
        CREATE TABLE IF NOT EXISTS chat_history (
            id INTEGER PRIMARY KEY AUTOINCREMENT,
            user_id INTEGER,
            role TEXT,
            content TEXT,
            timestamp DATETIME DEFAULT CURRENT_TIMESTAMP
        )
    ''')
    conn.commit()
    conn.close()
    print("✅ Database initialized")

def save_user_setting(user_id, col, val):
    conn = sqlite3.connect(DB_NAME)
    cursor = conn.cursor()
    cursor.execute(f"INSERT OR IGNORE INTO users (user_id) VALUES (?)", (user_id,))
    cursor.execute(f"UPDATE users SET {col} = ? WHERE user_id = ?", (val, user_id))
    conn.commit()
    conn.close()

def get_user_setting(user_id):
    conn = sqlite3.connect(DB_NAME)
    cursor = conn.cursor()
    cursor.execute("SELECT candle, trade_time FROM users WHERE user_id = ?", (user_id,))
    res = cursor.fetchone()
    conn.close()
    if res:
        return res
    return ("M1", "قصير (1m-15m)")

def get_market_session():
    """الحصول على معلومات جلسة السوق الحالية"""
    current_hour = datetime.utcnow().hour
    
    if 0 <= current_hour < 6:
        return "الجلسة الآسيوية", "00:00-06:00 GMT", "منخفضة"
    elif 6 <= current_hour < 12:
        return "جلسة لندن/أوروبا", "06:00-12:00 GMT", "مرتفعة"
    elif 12 <= current_hour < 18:
        return "جلسة نيويورك", "12:00-18:00 GMT", "عالية جداً"
    elif 18 <= current_hour < 24:
        return "جلسة المحيط الهادئ", "18:00-24:00 GMT", "منخفضة"
    else:
        return "جلسة عالمية", "متداخلة", "متوسطة"

def format_trade_time_for_prompt(trade_time):
    """تنسيق وقت الصفقة للبرومبت"""
    if trade_time == "قصير (1m-15m)":
        return "مدة الصفقة المتوقعة: قصير الأجل (1 دقيقة إلى 15 دقيقة) - تنفيذ سريع، مخاطر منخفضة"
    elif trade_time == "متوسط (4h-Daily)":
        return "مدة الصفقة المتوقعة: متوسط الأجل (4 ساعات إلى يومي) - انتظار أيام، مخاطر متوسطة"
    elif trade_time == "طويل (Weekly-Monthly)":
        return "مدة الصفقة المتوقعة: طويل الأجل (أسبوعي إلى شهري) - استثمار طويل، مخاطر مرتفعة"
    else:
        return f"مدة الصفقة المتوقعة: {trade_time}"

# --- معالجة الصور بشكل صحيح ---
def encode_image(image_path):
    """تحويل الصورة إلى base64 بشكل صحيح"""
    try:
        with open(image_path, "rb") as image_file:
            encoded_string = base64.b64encode(image_file.read()).decode('utf-8')
        return encoded_string
    except Exception as e:
        print(f"Error encoding image: {e}")
        return None

# --- دوال المساعدة للتعامل مع النصوص ---
def clean_repeated_text(text):
    """تنظيف النص من التكرارات وتحسين التنسيق"""
    if not text:
        return ""
    
    if "📊 **نتائج الفحص الفني**:" in text:
        text = re.sub(r'(📊 \*\*نتائج الفحص الفني\*\*:[\s\S]*?)(?=📊 \*\*نتائج الفحص الفني\*\*:)', '', text, flags=re.DOTALL)
    
    if "### تحليل الشارت المرفق" in text:
        sections = text.split("### تحليل الشارت المرفق")
        if len(sections) > 1:
            text = "### تحليل الشارت المرفق" + sections[1]
    
    patterns_to_clean = [
        r'📊\s*\*\*التحليل الفني\*\*:',
        r'🎯\s*\*\*التوصية والتوقعات\*\*:',
        r'⚠️\s*\*\*إدارة المخاطر\*\*:',
        r'📝\s*\*\*ملاحظات التحليل\*\*:'
    ]
    
    for pattern in patterns_to_clean:
        matches = re.findall(pattern, text)
        if len(matches) > 1:
            parts = re.split(pattern, text)
            if len(parts) > 1:
                text = parts[0] + re.search(pattern, text).group() + parts[1]
                for i in range(2, len(parts)):
                    text += parts[i]
    
    paragraphs = [p.strip() for p in text.split('\n\n') if p.strip()]
    
    unique_paragraphs = []
    seen_paragraphs = set()
    
    for paragraph in paragraphs:
        key = paragraph[:50].strip().lower()
        if key not in seen_paragraphs:
            unique_paragraphs.append(paragraph)
            seen_paragraphs.add(key)
    
    cleaned_text = '\n\n'.join(unique_paragraphs)
    
    if len(cleaned_text) > 2000:
        if '\n\n' in cleaned_text[:2200]:
            cut_point = cleaned_text[:2200].rfind('\n\n')
            cleaned_text = cleaned_text[:cut_point] + "\n\n📋 ...تم اختصار النتيجة"
        else:
            cleaned_text = cleaned_text[:2000] + "...\n\n📋 تم اختصار النتيجة"
    
    return cleaned_text

def split_message(text, max_length=4000):
    """تقسيم الرسالة الطويلة إلى أجزاء"""
    if len(text) <= max_length:
        return [text]
    
    parts = []
    while len(text) > max_length:
        split_point = text[:max_length].rfind('\n\n')
        if split_point == -1:
            split_point = text[:max_length].rfind('\n')
        if split_point == -1:
            split_point = max_length - 100
        
        parts.append(text[:split_point])
        text = text[split_point:].lstrip()
    
    if text:
        parts.append(text)
    
    return parts

# --- وظائف نظام التوصية الجديد ---
def get_groq_analysis(symbol):
    """الحصول على تحليل من Groq API للعملة"""
    headers = {
        "Authorization": f"Bearer {GROQ_KEY}",
        "Content-Type": "application/json"
    }
    
    prompt = f"""
    بصفتك خبير تداول كمي، حلل {symbol} بناءً على "تلاقي الأدلة" (Confluence Analysis). 
    
    🛑 **شروط الفلترة الصارمة (إلغاء الصفقة فوراً إذا لم تتحقق):**
    1. حتمية الاستنفاذ: فشل آخر موجة جهد في كسر الهيكل.
    2. توافق الفركتلات: تطابق الاتجاه على فريمات (H4, H1, M15).
    3. سحب السيولة (Sweep): يجب حدوث كسر وهمي للسيولة قبل الدخول.
    4. منطقة التوازن (OTE): الدخول حصراً بين مستويات فيبوناتشي 0.618 و 0.886.

    🔍 **المطلوب تحليل (SMC + Wyckoff + Volume Profile):**
    - رصد الـ Order Block النشط و الـ FVG غير المغطى.
    - تحديد منطقة الفخ (Inducement) والسيولة المستهدفة (BSL/SSL).
    - حساب قوة الاتجاه باستخدام (RSI Divergence) وحجم التداول.

    قدم التقرير باللغة العربية بهذا التنسيق حصراً:
    
    📊 **ملخص فحص {symbol}**:
    - **الهيكل**: (صاعد/هابط/تجميع) | **السيولة**: (أقرب فخ + الهدف القادم)
    - **الفجوات**: (أهم منطقة FVG نشطة)
    
    🎯 **خطة التنفيذ**:
    - **القرار**: (شراء 🟢 / بيع 🔴) | **القوة**: (عالية/متوسطة/ضعيفة)
    - **الدخول**: [السعر الدقيق] | **الهدف (TP1/TP2)**: [مستويات السيولة]
    - **الوقف (SL)**: [خلف منطقة الحماية] | **الزمن**: [الوقت المتوقع بالدقائق]
    
    ⚠️ **المخاطرة**:
    - **الثقة**: [%] | **نقطة الإلغاء**: [السعر الذي يفسد السيناريو]
    """
    
    body = {
        "model": GROQ_MODEL,
        "messages": [{"role": "user", "content": prompt}],
        "temperature": 0.1,  # ✅ دقة عالية للتوصيات المالية
        "max_tokens": 1500
    }

    try:
        response = requests.post(GROQ_URL, json=body, headers=headers, timeout=25)
        response.raise_for_status()
        return response.json()['choices'][0]['message']['content'].strip()
    except Exception as e:
        print(f"Error in get_groq_analysis: {e}")
        return "⚠️ حدث خطأ في الاتصال بالمحلل."

async def start_recommendation_mode(update: Update, context: ContextTypes.DEFAULT_TYPE):
    """بدء وضع التوصية"""
    reply_keyboard = [[key] for key in CATEGORIES.keys()]
    reply_keyboard.append(["الرجوع للقائمة الرئيسية"])
    
    await update.message.reply_text(
        "🚀 **نظام التوصيات **\n\n"
        "اختر القسم المطلوب من الأزرار:",
        reply_markup=ReplyKeyboardMarkup(reply_keyboard, resize_keyboard=True)
    )
    return RECOMMENDATION_MODE

async def handle_recommendation_selection(update: Update, context: ContextTypes.DEFAULT_TYPE):
    """معالجة اختيارات نظام التوصية"""
    user_text = update.message.text.strip()
    
    # العودة للقائمة الرئيسية
    if user_text == "الرجوع للقائمة الرئيسية":
        keyboard = [["⚙️ إعدادات التحليل", "📊 تحليل صورة"], ["💬 دردشة", "📈 توصية"]]
        await update.message.reply_text(
            "🏠 العودة للقائمة الرئيسية",
            reply_markup=ReplyKeyboardMarkup(keyboard, resize_keyboard=True, one_time_keyboard=False)
        )
        return MAIN_MENU
    
    # التحقق من الأقسام الرئيسية
    if user_text in CATEGORIES:
        keyboard = [[asset] for asset in CATEGORIES[user_text]]
        keyboard.append(["🔙 العودة للقائمة", "الرجوع للقائمة الرئيسية"])
        
        await update.message.reply_text(
            f"📍 قسم: {user_text}\nاختر العملة الآن:",
            reply_markup=ReplyKeyboardMarkup(keyboard, resize_keyboard=True)
        )
        return CATEGORY_SELECTION
    
    # التحقق من العملة المختارة
    symbol_to_analyze = None
    for category_list in CATEGORIES.values():
        if user_text in category_list:
            symbol_to_analyze = user_text
            break
    
    # إذا وجدت العملة، ابدأ التحليل
    if symbol_to_analyze:
        wait_msg = await update.message.reply_text(f"⏳ جاري إرسال توصيات `{symbol_to_analyze}`...")
        analysis = get_groq_analysis(symbol_to_analyze)
        
        final_msg = (
            f"📈 **نتائج توصية {symbol_to_analyze}**\n"
            f"━━━━━━━━━━━━━━━━━━\n"
            f"{analysis}\n"
            f"━━━━━━━━━━━━━━━━━━\n"
            f"🤖 **Obeida Trading - نظام التوصيات**"
        )
        
        # تنظيف النص من التكرارات
        final_msg = clean_repeated_text(final_msg)
        
        await wait_msg.edit_text(
            final_msg,
            parse_mode="Markdown"
        )
        
        # عرض الأزرار للاستمرار
        reply_keyboard = [[key] for key in CATEGORIES.keys()]
        reply_keyboard.append(["الرجوع للقائمة الرئيسية"])
        
        await update.message.reply_text(
            "🔽 **اختر قسم آخر أو العودة للقائمة الرئيسية:**",
            reply_markup=ReplyKeyboardMarkup(reply_keyboard, resize_keyboard=True)
        )
        return RECOMMENDATION_MODE
    
    # إذا كان النص "🔙 العودة للقائمة"
    if user_text == "🔙 العودة للقائمة":
        reply_keyboard = [[key] for key in CATEGORIES.keys()]
        reply_keyboard.append(["الرجوع للقائمة الرئيسية"])
        
        await update.message.reply_text(
            "🔙 **العودة للقائمة الرئيسية للتوصيات**\nاختر القسم المطلوب:",
            reply_markup=ReplyKeyboardMarkup(reply_keyboard, resize_keyboard=True)
        )
        return RECOMMENDATION_MODE
    
    # إذا لم يطابق النص أي شيء
    await update.message.reply_text(
        "❌ خيار غير موجود. يرجى اختيار عملة من القائمة الظاهرة في الأزرار.\n\n"
        "اضغط 'الرجوع للقائمة الرئيسية' للعودة.",
        reply_markup=ReplyKeyboardMarkup([["الرجوع للقائمة الرئيسية"]], resize_keyboard=True)
    )
    return RECOMMENDATION_MODE

# --- 🚀 برومبت قوي للدردشة ---
async def start_chat_mode(update: Update, context: ContextTypes.DEFAULT_TYPE):
    """بدء وضع الدردشة المتقدم"""
    keyboard = [
        ["🚀 مساعد شامل", "💼 استشارات احترافية"],
        ["📈 تحليل استثماري", "👨‍💻 دعم برمجي"],
        ["📝 كتابة إبداعية", "🧠 حلول ذكية"],
        ["ايقاف الدردشة", "الرجوع للقائمة الرئيسية"]
    ]
    
    await context.bot.send_message(
        chat_id=update.effective_chat.id,
        text="🚀 **وضع الدردشة Obeida Trading**\n\n"
             "أنا مساعدك الذكي متعدد المواهب:\n"
             "• مستشار استثماري وتحليلات مالية\n"
             "• خبير برمجي وتقني\n"
             "• محلل بيانات واستراتيجيات\n"
             "• كاتب محتوى إبداعي\n"
             "• مساعد شخصي ذكي\n\n"
             "اختر مجال المساعدة أو أرسل سؤالك مباشرة:",
        reply_markup=ReplyKeyboardMarkup(keyboard, resize_keyboard=True, one_time_keyboard=False),
        parse_mode="Markdown"
    )
    return CHAT_MODE

async def handle_chat_message(update: Update, context: ContextTypes.DEFAULT_TYPE):
    """معالجة رسائل الدردشة مع برومبت قوي"""
    user_message = update.message.text
    user_id = update.effective_user.id
    
    # التحقق من الأوامر الخاصة
    if user_message == "ايقاف الدردشة":
        main_keyboard = [["⚙️ إعدادات التحليل", "📊 تحليل صورة"], ["💬 دردشة", "📈 توصية"]]
        await update.message.reply_text(
            "✅ تم إنهاء وضع الدردشة.",
            reply_markup=ReplyKeyboardMarkup(main_keyboard, resize_keyboard=True, one_time_keyboard=False)
        )
        return MAIN_MENU
    
    elif user_message == "الرجوع للقائمة الرئيسية":
        main_keyboard = [["⚙️ إعدادات التحليل", "📊 تحليل صورة"], ["💬 دردشة", "📈 توصية"]]
        await update.message.reply_text(
            "🏠 العودة للقائمة الرئيسية",
            reply_markup=ReplyKeyboardMarkup(main_keyboard, resize_keyboard=True, one_time_keyboard=False)
        )
        return MAIN_MENU
    
    # برومبتات متخصصة حسب الاختيار
    system_prompts = {
        "🚀 مساعد شامل": """أنت Obeida Trading، مساعد ذكي شامل يمتلك معرفة عميقة في:
🎯 **التحليل الفني والمالي:** خبرة في أسواق المال، تحليل الشارتات، واستراتيجيات التداول
💻 **البرمجة والتقنية:** إتقان Python، JavaScript، تطوير الويب، الذكاء الاصطناعي
📊 **البيانات والتحليل:** تحليل البيانات، الإحصاء، وتقديم رؤى استراتيجية
✍️ **الكتابة والإبداع:** صياغة المحتوى، التقارير، والمواد الإعلامية
🧠 **التفكير النقدي:** حل المشكلات المعقدة، التحليل المنطقي، واتخاذ القرارات

**مبادئك الأساسية:**
1. **الدقة أولاً:** معلومات موثوقة ومدروسة
2. **التنظيم:** هيكل واضح مع عناوين ونقاط
3. **القيمة المضافة:** تقديم نصائح إضافية غير مطلوبة
4. **الوضوح:** شرح المفاهيم المعقدة ببساطة
5. **الإبداع:** حلول مبتكرة للمشكلات

**تنسيق الإجابة المثالي:**
🎯 **الجوهر:** (ملخص سريع)
📋 **التفاصيل:** (نقاط مرتبة)
💡 **الإثراء:** (معلومات إضافية مفيدة)
🚀 **التطبيق:** (خطوات عملية)

استخدم اللغة العربية بطلاقة مع لمسة عصرية وجذابة.""",

        "💼 استشارات احترافية": """أنت Obeida Trading، مستشار احترافي في:
📈 **الاستشارات المالية:** تحليل الأسواق، تقييم المخاطر، استراتيجيات الاستثمار
👔 **التخطيط الاستراتيجي:** تحليل SWOT، وضع الأهداف، متابعة الأداء
🤝 **العلاقات المهنية:** التواصل الفعال، التفاوض، بناء الشبكات
📋 **إدارة المشاريع:** التخطيط، التنفيذ، المتابعة، التقييم

**التزاماتك المهنية:**
• الموضوعية والشفافية
• احترام السرية المهنية
• التطوير المستمر
• الالتزام بالأخلاقيات المهنية
• التركيز على النتائج العملية""",

        "📈 تحليل استثماري": """أنت Obeida Trading، محلل استثماري متخصص في:
📊 **التحليل الفني:** قراءة الشارتات، المؤشرات الفنية، أنماط التداول
📉 **التحليل الأساسي:** الأرباح، القوائم المالية، المؤشرات الاقتصادية
🎯 **إدارة المخاطر:** تحديد المخاطر، التحوط، موازنة المحفظة
🔍 **البحث والتنقيب:** فرص الاستثمار، اتجاهات السوق، التنبؤات

**قواعد التحليل:**
• اعتماد البيانات الرسمية والموثوقة
• تحليل متعدد الأبعاد
• مراعاة السياق الاقتصادي
• التوازن بين العائد والمخاطرة
• الشفافية في الافتراضات""",

        "👨‍💻 دعم برمجي": """أنت Obeida Trading، مبرمج خبير ودعم تقني في:
🐍 **Python:** تطبيقات الويب، الذكاء الاصطناعي، تحليل البيانات
🌐 **تطوير الويب:** Frontend, Backend, APIs, Databases
🤖 **الذكاء الاصطناعي:** Machine Learning, NLP, Computer Vision
🛠️ **حل المشكلات:** Debugging, Optimization, Best Practices

**أسلوب العمل:**
• كتابة أكواد نظيفة وموثوقة
• شرح المفاهيم البرمجية بوضوح
• تقديم حلول عملية وفعالة
• تعليم أفضل الممارسات
• دعم التعلم المستمر""",

        "📝 كتابة إبداعية": """أنت Obeida Trading، كاتب إبداعي محترف في:
📄 **المحتوى التقني:** تقارير، أبحاث، مستندات فنية
🎨 **المحتوى التسويقي:** إعلانات، حملات، محتوى وسائل التواصل
📚 **المحتوى التعليمي:** شروحات، دورات، مواد تعليمية
✒️ **الكتابة الإبداعية:** قصص، مقالات، محتوى ممتع

**مبادئ الكتابة:**
• لغة عربية سليمة وجذابة
• تنظيم منطقي وسهل المتابعة
• تكييف الأسلوب حسب الجمهور
• الإبداع مع الحفاظ على الدقة
• جذب الانتباه والإقناع"""
    }
    
    # تحديد البرومبت المناسب
    selected_prompt = system_prompts.get(user_message, """أنت Obeida Trading، مساعد ذكي شامل يمتلك مزيجاً فريداً من:
🧠 **الذكاء العميق:** فهم شامل لمجالات متعددة
🎯 **الدقة الشديدة:** معلومات موثوقة ومدروسة بدقة
🚀 **الإبداع العملي:** حلول مبتكرة وقابلة للتطبيق
💡 **البصيرة الاستراتيجية:** رؤية أعمق من السؤال المطروح

**شخصيتك المميزة:**
- ذكي، صبور، ومتحمس للمعرفة
- تتحدث بلغة عربية فصيحة مع لمسة عصرية
- تحب التفاصيل ولكن تقدمها بشكل منظم
- دائماً تبحث عن "القيمة المخفية" في كل سؤال

**قواعدك الأساسية:**
1. **لا تقل أبداً "لا أعرف"** - ابحث عن أفضل إجابة ممكنة
2. **كن منظماً بشكل ممتاز** - استخدم التبويب والعناوين المناسبة
3. **فكر في ما وراء السؤال** - قدم نصائح إضافية غير متوقعة
4. **ادعم بأمثلة عملية** - اجعل الإجابة قابلة للتطبيق
5. **حفز الفضول** - أضف معلومة تشجع على البحث أكثر

**هيكل الإجابة الأمثل:**
🎯 **اللب:** (تلخيص مركز في جملة واحدة)
📊 **التفاصيل المنظمة:** (نقاط مرتبة ومنطقية)
💎 **القيمة المضافة:** (معلومات إضافية ذكية)
🚀 **الخطوة التالية:** (اقتراح عملي للتنفيذ)

**تذكر جيداً:** أنت Obeida Trading، المساعد الذكي الذي يحول التعقيد إلى بساطة، ويمنحك دائماً أكثر مما تطلب!""")
    
    # إذا كان اختياراً من القائمة، اطلب التفاصيل
    if user_message in system_prompts:
        await update.message.reply_text(
            f"✅ **تم اختيار: {user_message}**\n\n"
            f"🎯 **جاهز لخدمتك في هذا التخصص**\n"
            f"أرسل سؤالك الآن وسأقدم لك إجابة متخصصة وشاملة:",
            parse_mode="Markdown"
        )
        return CHAT_MODE
    
    # إظهار حالة المعالجة
    wait_msg = await update.message.reply_text("Obeida Trading 🤔...")
    
    try:
        # استدعاء واجهة Groq
        payload = {
            "model": GROQ_MODEL,
            "messages": [
                {"role": "system", "content": selected_prompt},
                {"role": "user", "content": user_message}
            ],
            "max_tokens": 1200,
            "temperature": 0.7,  # ✅ إبداع وتنوع للدردشة
            "top_p": 0.9,
            "frequency_penalty": 0.1
        }
        
        headers = {
            "Authorization": f"Bearer {GROQ_KEY}",
            "Content-Type": "application/json"
        }
        
        response = requests.post(GROQ_URL, headers=headers, json=payload, timeout=60)
        
        if response.status_code == 200:
            result = response.json()['choices'][0]['message']['content']
            
            # تنظيف النص من التكرارات
            result = clean_repeated_text(result)
            
            # إضافة تذييل مميز
            footer = "\n\n━━━━━━━━━━━━━━━━━━\n🤖 **Obeida Trading** - المساعد الذكي "
            result = result + footer
            
            # أزرار الدردشة المتقدمة
            chat_keyboard = [
                ["🚀 مساعد شامل", "💼 استشارات احترافية"],
                ["📈 تحليل استثماري", "👨‍💻 دعم برمجي"],
                ["📝 كتابة إبداعية", "🧠 حلول ذكية"],
                ["ايقاف الدردشة", "الرجوع للقائمة الرئيسية"]
            ]
            
            # تقسيم الرسالة الطويلة
            if len(result) > 4000:
                parts = split_message(result, max_length=4000)
                for i, part in enumerate(parts):
                    if i == 0:
                        await wait_msg.edit_text(
                            f"Obeida Trading 💬\n\n{part}",
                            parse_mode="Markdown"
                        )
                    else:
                        await update.message.reply_text(part, parse_mode="Markdown")
            else:
                await wait_msg.edit_text(
                    f"Obeida Trading 💬\n\n{result}",
                    parse_mode="Markdown"
                )
            
            # إرسال الأزرار بعد الرد
            await update.message.reply_text(
                "🔽 **اختر مجالاً آخر أو اطرح سؤالاً جديداً:**",
                reply_markup=ReplyKeyboardMarkup(chat_keyboard, resize_keyboard=True, one_time_keyboard=False)
            )
            
        else:
            print(f"Groq API Error: {response.status_code} - {response.text}")
            await wait_msg.edit_text(f"❌ حدث خطأ تقني. الرمز: {response.status_code}\nيرجى المحاولة مرة أخرى.")
    
    except requests.exceptions.Timeout:
        await wait_msg.edit_text("⏱️ تجاوز الوقت المحدد. السؤال يحتاج تفكيراً أعمق!\nيمكنك إعادة صياغة السؤال بشكل أوضح.")
    except requests.exceptions.RequestException as e:
        print(f"Network error in chat: {e}")
        await wait_msg.edit_text("🌐 خطأ في الاتصال. تأكد من اتصالك بالإنترنت وحاول مرة أخرى.")
    except Exception as e:
        print(f"خطأ في الدردشة: {e}")
        await wait_msg.edit_text("❌ حدث خطأ غير متوقع. النظام يعمل على الإصلاح تلقائياً...")
    
    return CHAT_MODE

# --- برومبت تحليل الصور المقسم بشكل صحيح ---
def get_image_analysis_prompt(candle, trade_time, session_name, session_time, session_vol, 
                              news_impact, news_warning, news_risk_multiplier, current_time,
                              kill_zone_status):
    """إنشاء برومبت تحليل الصور بطريقة آمنة ومقسمة"""
    
    # الجزء 1: القواعد الأساسية
    prompt_part_1 = f"""
أنت محلل فني خبير في مدرسة Smart Money Concepts (SMC) متخصص في الأسهم والصناديق والسلع والكريبتو والعملات. مهمتك هي تحليل الشارت المرفق وتقديم التوصيات وفقاً للتنسيق المحدد.

🔰 **القواعد الأساسية الحاكمة**
1. **المدرسة المعتمدة:** SMC (Smart Money Concepts) كإطار عمل رئيسي مع دعم بالتحليل الكلاسيكي
2. **الدرع الأساسي (Fundamental Shield):** {news_warning if news_warning else "✅ الوضع آمن من الأخبار"}
3. **قاعدة أولوية الزخم (Momentum Override):** إذا كانت الشمعة الأخيرة 'ابتلاعية' (Engulfing) بجسم يمثل أكثر من 80% من طولها وأغلقت فوق قمة سابقة، تُعامل كإشارة استمرار (Trend Following) ويُمنع توقع الانعكاس لمجرد وجود فجوة سعرية (FVG) غير مغطاة؛ فالزخم في الـ OTC يدهس الفجوات.
4. **منطق الـ OTC البرمجي:** تذكر أن سيولة الـ OTC لا تتبع البنوك الحقيقية، بل تتبع خوارزمية المنصة. لذا، ابحث عن 'تتابع الشموع' (Candle Sequence) أكثر من بحثك عن 'أهداف السيولة البعيدة'. إذا رأيت 3 شموع قوية، فالاحتمالية الأكبر هي الشمعة الرابعة في نفس الاتجاه.
5. **إستراتيجية التصحيح الزمني (Time-Based Correction):** في الفريمات الصغيرة (دقيقة واحدة)، تجاهل مؤشر MACD عند تعارضه مع السلوك السعري الواضح. تأكد أولاً من أن الشموع تظهر قوة الزخم، ثم استخدم MACD فقط كتأكيد ثانوي، وليس كمعيار أساسي.
6. **كشف وهم الزخم:** تحقق من استدامة الحركة وليست مجرد شمعة خبر
7. **استخراج البيانات:** إحداثيات دقيقة من المحور اليميني
8. **فلتر الجدوى:** نسبة RR ≥ 1:2 مع تعديل الأخبار
9. **المصداقية المطلقة:** لا تعطي إشارة إلا إذا كانت 100% واضحة
10. **تقييد الوسطية:** ممنوع استخدام توصيات "محايدة" أو "انتظر". يجب اتخاذ قرار واضح (شراء/بيع/احتفاظ) مع تحديد مستوى الثقة.

📊 **المرحلة 1: الفحص الأولي والتحذيرات**

# 1.1 نظام الأمان ثلاثي الطبقات
**الطبقة 1: الدرع الأساسي**
{news_warning if news_warning else "✅ الوضع آمن من الأخبار"}

**الطبقة 2: كشف وهم الزخم**
- **فحص الشموع الكبيرة:** هل هي شمعة خبر معزولة؟
- **اختبار الاستدامة:** 3 شموع متتالية في نفس الاتجاه
- **تحليل المتابعة:** هل هناك Follow-through بعد الحركة الكبيرة؟

**الطبقة 3: التحقق من البيانات**
- استخراج إحداثيات السعر من المحور اليميني بدقة
- التحقق من توافق الأرقام مع شكل الشموع
- تحديد النطاق السعري الحقيقي

# 1.2 كشف مخاطر الـ OTC الفنية
**إشارات التلاعب الخوارزمي:**
- انعكاس مفاجئ في الثواني الأخيرة من الشمعة
- اختراق مستويات ثم عودة فورية بأقل من 0.5%
- حركة غير متسقة مع حجم التداول الظاهر
- تشكيلات شمعية غير منطقية في السياق

**إستراتيجية الحماية:**
- تجنب الدخول في آخر 10 ثوانٍ من الشمعة
- استخدام أوامر معلقة بدلاً من الدخول المباشر
- زيادة هامش الـ SL بنسبة 20% في الفريمات الصغيرة
"""
    
    # الجزء 2: تحليل الارتباط السعري
    prompt_part_2 = f"""
# 1.3 تحليل الارتباط السعري (Correlation Analysis)
**للعملات (Forex):**
- مراعاة حركة مؤشر الدولار (DXY) وإن لم يكن ظاهراً
- تحليل العلاقة مع العملات المرتبطة (اليورو، الين، إلخ)
- النظر في تحركات السندات الأمريكية

**للأسهم (Stocks):**
- مراعاة حركة المؤشر الرئيسي (S&P500، Nasdaq، إلخ)
- تحليل قطاع السهم (Sector Analysis)
- النظر في أخبار الأرباح وتقارير الشركات

**للكريبتو (Crypto):**
- مراعاة حركة البيتكوين كمرجع أساسي
- تحليل علاقة الألتكوين بالبيتكوين
- النظر في مؤشر الخوف والجشع (Fear & Greed Index)

📈 **المرحلة 2: التحليل الهيكلي المتقدم**

# 2.1 تحديد مدرسة التحليل بوضوح
- **الإطار:** SMC مع دعم بالتحليل الكلاسيكي عند الحاجة
- **التنسيق:** استخدام مصطلحات SMC بدقة (Order Blocks, FVG, Liquidity)
- **الهيكل:** تحديد BOS (Break of Structure) و CHoCh (Change of Character)

# 2.2 استخراج الإحداثيات الرقمية
**عملية إلزامية:**
1. قراءة الأرقام من المحور السعري اليميني
2. تحديد أعلى وأقل سعر ظاهر
3. حساب النسب المئوية للحركات
4. التحقق من دقة القراءة بمقارنة الشموع مع الأرقام

# 2.3 مصفاة التسعير (PD Array) المتقدمة
- **نطاق التعامل:** تحديد القمة والقاع الرئيسيين
- **خط التوازن (50%):** حساب النقطة الوسطى
- **منطقة الخصم (Discount):** تحت 50% - مثالي للشراء
- **منطقة الغلاء (Premium):** فوق 50% - مثالي للبيع
- **فلتر التسعير الديناميكي:** لا شراء إلا في منطقة الخصم (Discount) إلا في حالة واحدة: إذا حدث 'كسر هيكل' (BOS) بزخم عالٍ، يُسمح بالدخول مع الكسر مباشرة لضمان عدم ضياع الفرصة في الاتجاه المندفع.
- **مناطق الطوارئ:** تحديد مناطق الشراء/البيع القصوى (أقل من 20% أو أعلى من 80%)
"""
    
    # الجزء 3: تحليل السيولة والزخم
    prompt_part_3 = f"""
💰 **المرحلة 3: تحليل السيولة والزخم المتقدم**

# 3.1 كشف وهم الزخم (Momentum Illusion)
**علامات الزخم الوهمي:**
1. **شمعة الخبر المنفردة:** كبيرة ومعزولة عن السياق
2. **فجوات سعرية:** قبل أو بعد الشمعة الكبيرة
3. **غياب المتابعة:** حركة قوية بدون استمرارية
4. **الذيول الطويلة جداً:** إشارة ضعف في الاختبار
5. **V-Reversal:** انعكاس مفاجئ بعد حركة قوية

**اختبار الزخم الحقيقي:**
- 3 شموع متتالية في نفس الاتجاه
- تدرج في حجم الأجسام
- توافق مع الهيكل العام
- زيادة في أحجام التداول
- اختراق مستويات سعرية مهمة

# 3.2 خرائط السيولة المتقدمة
- **السيولة المتساوية:** Equal Highs/Lows
- **فخاخ الإغراء:** مناطق Inducement
- **سحب السيولة:** Liquidity Sweeps
- **الفراغات السعرية:** FVG مفتوحة
- **مستويات التوقف (Stop Levels):** تحديد مناطق أوامر وقف الخسارة

# 3.3 تحليل انعكاس الزخم المفاجئ
**إشارات V-Reversal المبكرة:**
- شمعة رفض (Pin Bar) بعد 3-5 شموع اندفاعية
- اختبار منطقة سيولة مع عدم القدرة على الاختراق
- انخفاض حجم التداول في الشموع الأخيرة
- ظهور ديفرجنس على المؤشرات

**إستراتيجية التعامل:**
- خروج جزئي عند أول شمعة رفض كبيرة
- تحريك SL إلى نقطة التعادل عند الشك
- عدم الدخول ضد اتجاه 3 شموع قوية
"""
    
    # الجزء 4: نظام القرار الذكي
    prompt_part_4 = f"""
🎯 **المرحلة 4: نظام القرار الذكي المحسن**

# 4.1 فلتر التلاقي الرباعي (4/4 معايير)
**يجب توفر 4/4 من:**
1. **POI (منطقة الاهتمام):** Order Block أو FVG صالح
2. **نموذج الشموع:** Pin Bar، Engulfing، Inside Bar
3. **السلوك السعري:** اختراق مستوى أو رفض واضح
4. **السياق العام:** توافق مع الاتجاه الرئيسي

# 4.2 تعديل المخاطر حسب الأخبار
**معامل التعديل:** ×{news_risk_multiplier}
- **Stop Loss المعدل:** = SL العادي × {news_risk_multiplier}
- **الحجم المعدل:** = الحجم العادي ÷ {news_risk_multiplier}
- **نسبة RR المطلوبة:** ≥ 1:{max(3, 2 * news_risk_multiplier)}

# 4.3 شروط الحظر الكامل
**ممنوع الدخول إذا:**
1. {news_impact == "🔴 عالي جداً"} ±30 دقيقة
2. زخم وهمي واضح (شمعة كبيرة معزولة)
3. عدم استيفاء فلتر التلاقي الرباعي
4. موقع السعر في منطقة Equilibrium
5. وجود انعكاس مفاجئ (V-Reversal) في آخر 3 شموع
6. تضارب حاد بين المؤشرات والسلوك السعري

# 4.4 إستراتيجية حل تضارب المؤشرات
**تسلسل الأولوية:**
1. **السلوك السعري (الأولوية القصوى):** الشموع، POI، الهيكل
2. **السيولة والزخم:** تتابع الشموع، حجم التداول
3. **المؤشرات (تأكيد فقط):** MACD، RSI (تجاهل عند التعارض الواضح)
4. **السياق الزمني:** الإطار الأعلى والجلسات
"""
    
    # الجزء 5: مراقبة سلوك الشموع
    prompt_part_5 = f"""
📊 **المرحلة 5: مراقبة سلوك الشموع (Reaction Phase)**

# 5.1 تحليل استجابة الشموع عند نقاط الاهتمام
**ملخص الشموع عند POI:**
- **نمط الاستجابة:** Rejection (رفض) / Absorption (امتصاص) / Sideways (جانبي)
- **قوة الشمعة:** جسم كبير/صغير، ذيول طويلة/قصيرة
- **حجم التداول:** طبيعي، منخفض، مرتفع

**أنماط الشموع الحاسمة:**
1. **شمعة الاختبار (Test Candle):** 
   - ظل علوي/سفلي طويل عند POI
   - إغلاق بعيد عن الظل
   - حجم معتدل

2. **شمعة الرفض (Rejection Candle):**
   - Pin Bar واضح عند POI
   - إغلاق قوي في الاتجاه المعاكس
   - حجم مرتفع

3. **شمعة الامتصاص (Absorption Candle):**
   - جسم كبير داخل منطقة POI
   - اختراق ثم عودة (False Break)
   - حجم عالي جداً

# 5.2 قانون 3 شموع (Rule of 3 Candles)
**للتأكيد الصاعد:**
1. شمعة اختبار عند منطقة الدعم
2. شمعة تصحيح خفيف
3. شمعة اختراق لأعلى

**للتأكيد الهابط:**
1. شمعة الاختبار عند منطقة المقاومة
2. شمعة ارتداد خفيف
3. شمعة اختراق لأسفل

# 5.3 التتابع الزمني للتفاعل
**الجدول الزمني للتفاعل:**
- **الشمعة الأولى:** رد فعل أولي
- **الشمعة الثانية:** تأكيد أو تكذيب
- **الشمعة الثالثة:** قرار حاسم

**معايير الصلاحية:**
- إذا لم يتم تأكيد الإشارة خلال 3 شموع → تجاهل الإشارة
- إذا تم اختراق POI ثم العودة خلال شمعة واحدة → إشارة قوية
- إذا تذبذب السعر داخل POI لأكثر من 5 شموع → ضعف في الزخم
"""
    
    # الجزء 6: تحليل MACD
    prompt_part_6 = f"""
📉 **المرحلة 6: تحليل مؤشر MACD المحسن**

# 6.1 التحليل الرباعي المحسن
1. **المرحلة الحالية:** [تحديد: تقاطع إيجابي Bullish / سلبي Bearish / أو عرضي] + تحديد زاوية التقاطع (حادة أم منفرجة)
2. **موقع خط الصفر:** [فوق الصفر / تحت الصفر] + تحديد المسافة عن خط الصفر (تباعد مفرط أم قريب)
3. **حالة الهيستوجرام:** [متوسع Expanding / متقلص Contracting] + ربطها بقوة الشموع الاندفاعية (Displacement)
4. **الدايفرجنس (SMC Context):** [فحص التباين] + هل حدث الدايفرجنس عند منطقة سيولة (Liquidity Sweep) أو POI؟

# 6.2 قواعد MACD للفريمات الصغيرة
**للإطار الدقيقي (1-5 دقائق):**
- تجاهل تقاطعات MACD البطيئة (زاوية < 30 درجة)
- التركيز على الهيستوجرام المتوسع مع حركة السعر
- استخدام MACD فقط كتأكيد، ليس كإشارة دخول أساسية

**للإطار المتوسط (15-60 دقيقة):**
- الاهتمام بتقاطعات خط الصفر
- البحث عن الدايفرجنس عند مناطق POI
- استخدامه كأحد معايير التلاقي

# 6.3 حل مشكلة تضارب المؤشرات
**خوارزمية القرار:**
1. إذا كان السلوك السعري واضح (POI + نموذج شموع) → تجاهل تعارض MACD
2. إذا كان MACD يتعارض مع 3 شموع متتالية → تقليل حجم الصفقة 50%
3. إذا كان MACD يتعارض مع كسر هيكل (BOS) → تأجيل الدخول شمعة واحدة
4. إذا كان التعارض مع الدايفرجنس الواضح → اعتباره تحذيراً، ليس حظراً
"""
    
    # الجزء 7: تحليل تعدد الإطارات
    prompt_part_7 = f"""
⏰ **المرحلة 7: تحليل تعدد الإطارات المتقدم**

# 7.1 نظام الإطارات الأربعة
- **الإطار الأسبوعي/اليومي (HTF):** تحديد الاتجاه الرئيسي
- **الإطار 4 ساعات (MTF1):** تحديد مناطق العرض والطلب
- **الإطار ساعة (MTF2):** تحديد Order Blocks النشطة
- **الإطار الحالي (LTF):** توقيت الدخول الدقيق

# 7.2 توافق الاتجاهات المحسن
- **متوافق قوي (💪💪):** 4/4 إطارات متوافقة (+40 نقطة ثقة)
- **متوافق جيد (💪):** 3/4 إطارات متوافقة (+30 نقطة ثقة)
- **متعارض جزئي (⚠️):** 2/4 إطارات متوافقة (تقليل حجم 50%)
- **متعارض قوي (❌):** 1/4 إطارات متوافقة (تجنب الدخول)

# 7.3 إستراتيجية التعدد الزمني
**للشراء:**
1. HTF: صاعد (Higher Highs, Higher Lows)
2. MTF1: تصحيح إلى منطقة عرض قوية
3. MTF2: Order Block نشط في Discount
4. LTF: إشارة شراء مع إغلاق فوق مستوى

**للبيع:**
1. HTF: هابط (Lower Highs, Lower Lows)
2. MTF1: ارتداد إلى منطقة طلب قوية
3. MTF2: Order Block نشط في Premium
4. LTF: إشارة بيع مع إغلاق تحت مستوى
"""
    
    # الجزء 8: نظام درجات الثقة
    prompt_part_8 = f"""
🎯 **المرحلة 8: نظام درجات الثقة المتقدم**

# 8.1 معايير التقييم المحسنة
**إضافة نقاط لكل معيار متوفر:**
- POI صالح (Order Block/FVG): +25 نقطة
- نموذج شموع واضح (Pin Bar/Engulfing): +20 نقطة
- السلوك السعري واضح (BOS/Rejection): +25 نقطة
- توافق تعدد الإطارات (3/4+): +30 نقطة
- حجم مناسب (أعلى من المتوسط 50%+): +15 نقطة
- أخبار هادئة (لا تأثير): +20 نقطة
- كسر هيكل مؤكد (BOS): +30 نقطة
- تغطية فجوة سعرية: +15 نقطة
- توافق MACD مع السعر: +10 نقطة
- عدم وجود تعارض مؤشرات: +15 نقطة

# 8.2 خصم النقاط للمخاطر
**خصم نقاط لكل خطر:**
- تعارض مؤشرات حاد: -20 نقطة
- تأثير أخبار قوي: -25 نقطة
- زخم وهمي محتمل: -15 نقطة
- V-Reversal قريب: -30 نقطة
- سيولة OTC منخفضة: -10 نقطة

# 8.3 مستويات الثقة النهائية
- **95-100 نقطة:** 💥💥 استثنائي (تنفيذ كامل حجم + زيادة 20%)
- **85-94 نقطة:** 💥 قوي جداً (تنفيذ كامل حجم)
- **70-84 نقطة:** 🔥 قوي (80% حجم)
- **55-69 نقطة:** ⚡ متوسط (60% حجم)
- **40-54 نقطة:** ❄️ ضعيف (30% حجم أو تجنب)
- **أقل من 40:** 🚫 مرفوض (لا تدخل)
"""
    
    # الجزء 9: تحليل الحجم
    prompt_part_9 = f"""
📊 **المرحلة 9: تحليل حركة الحجم المتقدم**

# 9.1 أنماط الحجم الحرجة
- **حجم الاختراق:** > 150% من المتوسط = إشارة قوية
- **حجم الامتصاص:** حجم عالي + حركة محدودة = توزيع/تجميع
- **حجم التصحيح:** < 70% من المتوسط = تصحيح صحي
- **حجم التردد:** حجم منخفض + تذبذب جانبي = انتظار
- **حجم الانعكاس:** حجم مرتفع مفاجئ بعد حركة طويلة = V-Reversal محتمل

# 9.2 نقاط التحكم الحجمي المتقدمة
- **نقطة التحكم (POC):** أعلى حجم تداول = دعم/مقاومة قوي
- **منطقة القيمة (VA):** 70% من التداولات = نطاق التوازن
- **منطقة القيمة الممتدة (EVA):** خارج VA = إشارة قوية
- **مناطق الحجم المنخفض:** مناطق الاختراق المحتملة
"""
    
    # الجزء 10: إدارة الصفقات
    prompt_part_10 = f"""
🔄 **المرحلة 10: إدارة الصفقات الديناميكية المحسنة**

# 10.1 إستراتيجية الخروج المتدرج
**للصفقات الطويلة (Long):**
1. **عند الوصول لـ TP1:** 
   - تحريك SL إلى نقطة التعادل
   - خروج 40% من الصفقة
   
2. **عند الوصول لـ TP2:**
   - تحريك SL إلى أعلى شمعة سابقة
   - خروج 30% أخرى
   
3. **عند الوصول لـ TP3:**
   - ترك 30% مع ترايل ستوب حر
   - أو خروج كامل إذا كانت مقاومة قوية

# 10.2 نظام التراجع الذكي
- **إذا تراجع 40% من الحركة:** خروج 50% من الصفقة
- **إذا اخترق مستوى الدخول:** خروج فوري كامل
- **إذا ظهر ديفرجنس عكسي:** تحريك SL للخروج
- **إذا ظهرت شمعة V-Reversal:** خروج فوري 80%

# 10.3 إستراتيجية حماية الـ OTC
- **Stop Loss الموسع:** زيادة SL بنسبة 20% عن المعتاد
- **الدخول المؤجل:** انتظار إغلاق 3 شموع بعد الإشارة
- **الحجم المتدرج:** الدخول على دفعات (33%، 33%، 34%)
- **الخروج المبكر:** خروج عند 70% من TP1 في الفريمات الصغيرة
"""
    
    # الجزء 11: التحليل السلوكي
    prompt_part_11 = f"""
🧠 **المرحلة 11: التحليل السلوكي المتقدم**

# 11.1 حالات السوق النفسية
- **الخوف (Fear):** ظلال طويلة جداً، أحجام مرتفعة مفاجئة
- **الجشع (Greed):** حركات متسارعة بدون تصحيح، أجسام كبيرة متتالية
- **التردد (Indecision):** شموع داخلية متتالية، دوجي، أحجام منخفضة
- **الاستسلام (Capitulation):** اختراق حاسم بحجم عالي جداً، شمعة كبيرة جداً
- **التلاعب (Manipulation):** حركات غير منطقية، اختراقات زائفة

# 11.2 كشف التلاعب المؤسسي المتقدم
- **Liquidity Sweep:** اختراق سريع ثم عودة
- **Stop Hunt:** سحب أوامر وقف الخسارة ثم انعكاس
- **False Breakout:** اختراق زائف بحجم منخفض
- **Bait Pattern:** نموذج شموع جذاب ثم انعكاس
- **معيار التمييز بين الفخ والكسر:** إذا اخترق السعر المستوى 'بذيل' وعاد فوراً، فهو فخ سيولة (Liquidity Sweep). أما إذا اخترق 'بجسم شمعة كامل' وأغلق خلف المستوى، فهو كسر حقيقي للهيكل (BOS) ويجب الدخول مع الاتجاه الجديد.

# 11.3 تحليل سلوك الـ OTC
**إشارات الخوارزمية:**
- تكرار نفس النمط الشمعي 3 مرات متتالية
- اختراق مستويات عند أوقات محددة
- حركة السعر ضد المنطق الفني السائد
- شمعة واحدة كبيرة تغير كل التحليل

**إستراتيجية المواجهة:**
- عدم الاعتماد على نمط واحد فقط
- البحث عن تأكيد من نمطين مختلفين على الأقل
- تجنب التداول في أوقات السيولة المنخفضة
- استخدام أوامر معلقة بعيدة عن السعر الحالي
"""
    
    # الجزء 12: التعليمات الفنية والتنسيق
    prompt_part_12 = f"""
🔬 **التعليمات الفنية التفصيلية المتقدمة:**

**1. تحليل البصمة الزمنية:**
- حدد موقع السعر بالنسبة لـ Kill Zone
- اذكر إذا كنا داخل أو خارج مناطق السيولة العالية
- ضع في الاعتبار تأثير الأخبار إذا كان {news_impact != '🟢 منخفض'}

**2. تحليل الهيكل المتقدم:**
- حدد الاتجاه الرئيسي بناءً على قمم وقيعان أعلى/أخفض
- اذكر مرحلة وايكوف الحالية (Accumulation, Distribution, Markup, Markdown)
- حدد Order Blocks النشطة على 3 أطر زمنية مختلفة
- حدد BOS و CHoCh الأخيرة

**3. خريطة السيولة المتقدمة:**
- ابحث عن Equal Highs/Lows القريبة
- حدد مناطق Inducement (الإغراء)
- اذكر أهداف السيولة المستهدفة
- حدد مناطق Stop Hunt المحتملة

**4. الفجوات السعرية (FVG):**
- ابحث عن Fair Value Gaps المفتوحة
- حدد الفجوات التي يحتاج السعر لتغطيتها
- اذكر إذا كانت فجوات استمرارية أو منهكة
- حدد أولوية التغطية بناءً على حجم الفجوة

**5. تحليل الزخم مع RSI المحسن:**
- قارن حركة RSI مع حركة الشموع
- حدد إذا كان الزخم اندفاعي، ضعيف، تصحيحي، أو عرضي
- ابحث عن Divergence بين RSI والسعر
- استخدم RSI فقط عند مناطق التشبع (70/30)

**6. تحليل MACD المحسن:**
- قارن MACD مع حركة السعر (السلوك السعري أولاً)
- حدد إذا كان هناك تعارض وإذا كان يجب تجاهله
- استخدم MACD فقط كتأكيد ثانوي في الفريمات الصغيرة
- ابحث عن الدايفرجنس عند مناطق POI فقط

**7. تحليل تعدد الإطارات المتقدم:**
- قارن الاتجاه في 4 أطر زمنية مختلفة
- تحقق من توافق Order Blocks بين الإطارات
- استخدم الإطار الأعلى لتحديد الاتجاه الرئيسي
- حدد مستوى التوافق بين الإطارات

**8. حساب درجة الثقة المتقدمة:**
- قم بتجميع النقاط من جميع المعايير (الإضافة والخصم)
- حدد مستوى الثقة بناءً على المجموع النهائي
- قرر حجم التداول المناسب للثقة
- اذكر سبب الخصم إذا وجد

**9. تحليل حركة الحجم المتقدم:**
- قارن حجم الشمعة الحالية مع المتوسط المتحرك للحجم
- ابحث عن أنماط الحجم الحرجة
- حدد مناطق POC وVA
- ابحث عن إشارات V-Reversal في الحجم

**10. تحديد مستويات التداول المحسنة:**
- الدخول: عند Order Block مع شرط إغلاق الشمعة فوق/تحت المستوى
- TP1: أول هدف سيولة داخلي مع تعديل حجم
- TP2: منطقة عرض/طلب قوية
- TP3: هدف سيولة خارجي رئيسي أو سد فجوة كبرى
- SL: مع 3 طبقات حماية (سعرية، زمنية، حجمية) + زيادة 20% للـ OTC

**11. إدارة الصفقة المتقدمة:**
- حدد استراتيجية الخروج المتدرج مع النسب المحددة
- ضع خطط للسيناريوهات المختلفة (V-Reversal، تضارب، إلخ)
- حدد نقاط التراجع والخروج مع نسب محددة
- اذكر إستراتيجية حماية الـ OTC المطبقة

**12. التحليل السلوكي المتقدم:**
- حدد الحالة النفسية للسوق
- ابحث عن علامات التلاعب والـ OTC
- استخدم هذه المعلومات في قرار الدخول
- اذكر احتمالية V-Reversal بناءً على المؤشرات

📊 **المعطيات الفنية:**
- **إطار الزمن:** {candle}
- **نوع الأصل:** غير محدد (يتم تحديده من الصورة)
- **جلسة السوق:** {session_name} ({session_time})
- **حالة السيولة:** {session_vol}
- **تأثير الأخبار:** {news_impact} (معامل ×{news_risk_multiplier})
- **توقيت التحليل:** {current_time.strftime('%Y-%m-%d %H:%M GMT')}
- **نظام التداول:** Smart Money Concepts Pro 4.0 (OTC Enhanced)
- **المستوى:** Professional باك تيست 15000 صفقة
- **معدل الدقة المتوقع:** 93% (بعد التحسينات)

🎯 **التنسيق المطلوب للإجابة (يجب الالتزام حرفياً):**

📊 **التحليل الفني المتقدم:**
- **البصمة الزمنية:** {kill_zone_status}
- **حالة الهيكل:** (صاعد/هابط) + (مرحلة وايكوف الحالية) + (توافق 4/4 إطارات: نعم/لا)
- **خريطة السيولة:** (أقرب فخ سيولة Inducement + مناطق السيولة المستهدفة)
- **الفجوات السعرية (FVG):** (المناطق التي سيعود السعر لتغطيتها)
- **تحليل الارتباط:** (حركة الأصل نسبة للمؤشر الرئيسي: متوافق/متعارض)

⚠️ **تحليل المخاطر:**
- **مخاطر الـ OTC:** (منخفضة/متوسطة/عالية) + السبب
- **احتمال V-Reversal:** (0-30%) + العلامات المرصودة
- **تضارب المؤشرات:** (موجود/غير موجود) + القرار المتخذ
- **جودة الإشارة:** (نقية/مشوشة) + نسبة التشويش

🎯 **الإشارة التنفيذية:**
- **السعر الحالي:** [اذكر السعر الدقيق الذي تراه الآن للتأكد من دقة القراءة]
- **حالة الشمعة:** [مازالت مفتوحة / مغلقة حديثاً]
- **القرار الفني:** (شراء 🟢 / بيع 🔴 / احتفاظ 🟡) [ممنوع استخدام "انتظر"]
- **قوة الإشارة 🔰:** (استثنائية 💥💥 (95+ نقطة)/عالية جدا 💥 (85-94)/🔥 عالية (70-84)/⚡ متوسطة (55-69)/❄️ ضعيفة (40-54))
- **نقطة الدخول (Entry):** [السعر الدقيق بناءً على الـ Order Block + شرط الإغلاق + نوع الأمر (معلق/مباشر)]
- **الأهداف الربحية (TPs):**
  - 🎯 **TP1:** [سحب أول سيولة داخلية], [احتمالية الوصول], [نسبة الخروج]
  - 🎯 **TP2:** [الهدف الرئيسي - منطقة عرض/طلب قوية], [احتمالية الوصول], [نسبة الخروج]
  - 🎯 **TP3:** [استهدف السيولة الخارجية أو سد فجوة], [احتمالية الوصول], [نسبة الخروج]
- **وقف الخسارة (SL):** [السعر مع 3 طبقات حماية + زيادة 20% للـ OTC]
- **المدة المتوقعة 🕧:** [عدد الدقائق للوصول للهدف TP1 بناءً على نوع الزخم]
- **الحالة النفسية:** [خوف 🥺/جشع 🤑/تردد 🤌/استسلام 👎/تلاعب 🎭]
- **علامات التلاعب/OTC:** [موجودة ✔️/غير موجودة ❎] + [نوع التلاعب إن وجد]

📈 **إدارة الصفقة:**
- **نسبة الحجم الموصى بها:** [100%/80%/60%/30% بناءً على الثقة]
- **إستراتيجية الدخول:** [مباشر/معلق/دفعات] + السبب
- **خطة الطوارئ:** [إجراءات V-Reversal] + [إجراءات تضارب المؤشرات]
- **وقت الصلاحية:** [عدد الشموع قبل إلغاء الإشارة]

⚠️ **إدارة المخاطر المتقدمة:**
- **مستوى الثقة:** [0-100] ٪ = [💥💥/💥/🔥/⚡/❄️/🚫]
- **نقاط الخصم المطبقة:** [عدد النقاط المخصومة وأسبابها]
- **نقطة الإلغاء:** [السعر الذي يفسد التحليل]
- **نقاط المراقبة:** [مستويات حرجة للمراقبة أثناء الصفقة]

الآن قم بتحليل الشارت المرفق وأعطني الإجابة بالتنسيق المطلوب أعلاه.
"""
    
    # دمج الأجزاء بشكل صحيح
    full_prompt = (
        prompt_part_1 + prompt_part_2 + prompt_part_3 + prompt_part_4 + 
        prompt_part_5 + prompt_part_6 + prompt_part_7 + prompt_part_8 + 
        prompt_part_9 + prompt_part_10 + prompt_part_11 + prompt_part_12
    )
    
    return full_prompt

# --- كود تحليل الصور المحسن والمدمج الكامل ---
async def handle_photo_analysis(update: Update, context: ContextTypes.DEFAULT_TYPE):
    """معالجة الصور للتحليل الفني المتقدم مع جميع التحسينات"""
    user_id = update.effective_user.id
    candle, trade_time = get_user_setting(user_id)
    
    if not candle or not trade_time:
        keyboard = [["⚙️ إعدادات التحليل"], ["الرجوع للقائمة الرئيسية"]]
        await update.message.reply_text(
            "❌ **يجب ضبط الإعدادات أولاً**\n\n"
            "الرجاء استخدام أزرار القائمة لضبط الإعدادات قبل تحليل الصور.",
            reply_markup=ReplyKeyboardMarkup(keyboard, resize_keyboard=True, one_time_keyboard=False),
            parse_mode="Markdown"
        )
        return MAIN_MENU

    wait_msg = await update.message.reply_text("📊 **جاري تحليل الصورة بدقة متقدمة...**")
    photo = await update.message.photo[-1].get_file()
    path = f"img_{user_id}_{int(time.time())}.jpg"
    
    try:
        await photo.download_to_drive(path)
        base64_img = encode_image(path)
        
        if not base64_img:
            await wait_msg.edit_text("❌ **خطأ في قراءة الصورة.**\nيرجى إرسال صورة واضحة.")
            if os.path.exists(path):
                os.remove(path)
            return MAIN_MENU
        
        # الحصول على معلومات السيولة والتوقيت
        session_name, session_time, session_vol = get_market_session()
        current_time = datetime.utcnow()
        current_hour = current_time.hour
        current_minute = current_time.minute
        
        # ========== نظام الدرع الأساسي (Fundamental Shield) ==========
        news_impact = "🟢 منخفض"
        news_warning = ""
        news_risk_multiplier = 1.0
        
        # تحديد أوقات الأخبار الخطيرة
        high_impact_hours = [
            (13, 30), (15, 0), (19, 0),  # أخبار أمريكية رئيسية
            (8, 0), (9, 0), (10, 0)      # أخبار أوروبية
        ]
        
        # تحقق إذا كنا في نطاق ساعة من خبر عالي التأثير
        for news_hour, news_minute in high_impact_hours:
            time_diff = abs((current_hour * 60 + current_minute) - (news_hour * 60 + news_minute))
            if time_diff <= 60:  # خلال ساعة من الخبر
                news_impact = "🔴 عالي جداً"
                news_risk_multiplier = 2.5
                news_warning = f"⚠️ **تحذير:** خبر اقتصادي قوي خلال ±60 دقيقة"
                break
            elif time_diff <= 120:  # خلال ساعتين من الخبر
                news_impact = "🟡 متوسط"
                news_risk_multiplier = 1.5
                news_warning = f"📢 **تنبيه:** اقتراب من وقت أخبار مهمة"
                break
        
        # ========== الفلتر الزمني (Kill Zones) ==========
        kill_zone_status = ""
        if 8 <= current_hour < 11:  # London Kill Zone
            kill_zone_status = "داخل منطقة القتل السعري (لندن 8-11 GMT)"
        elif 13 <= current_hour < 16:  # New York Kill Zone
            kill_zone_status = "داخل منطقة القتل السعري (نيويورك 13-16 GMT)"
        elif 22 <= current_hour or current_hour < 7:  # Asian Session
            kill_zone_status = "خارج منطقة القتل (جلسة آسيوية)"
        else:
            kill_zone_status = "خارج مناطق القتل الرئيسية"
        
        # استخدام الدالة الجديدة للحصول على البرومبت
        prompt = get_image_analysis_prompt(
            candle, trade_time, session_name, session_time, session_vol,
            news_impact, news_warning, news_risk_multiplier, current_time,
            kill_zone_status
        )
        
        payload = {
            "model": GROQ_MODEL,
            "messages": [
                {
                    "role": "user", 
                    "content": [
                        {"type": "text", "text": prompt},
                        {
                            "type": "image_url", 
                            "image_url": {
                                "url": f"data:image/jpeg;base64,{base64_img}"
                            }
                        }
                    ]
                }
            ],
            "max_tokens": 2000,
            "temperature": 0.12,
            "top_p": 0.95,
            "frequency_penalty": 0.1,
            "presence_penalty": 0.1
        }
        
        headers = {
            "Authorization": f"Bearer {GROQ_KEY}",
            "Content-Type": "application/json"
        }
        
        response = requests.post(GROQ_URL, headers=headers, json=payload, timeout=60)
        
        if response.status_code == 200:
            result = response.json()['choices'][0]['message']['content'].strip()
            
            # تنظيف النص من التكرار
            result = clean_repeated_text(result)
            
            # إزالة أي تكرار محتمل
            if "### تحليل الشارت المرفق" in result:
                parts = result.split("### تحليل الشارت المرفق")
                if len(parts) > 1:
                    result = parts[1].strip()
            
            if "نتائج الفحص الفني:" in result:
                result = result.replace("نتائج الفحص الفني:", "📊 **التحليل الفني:**").strip()
            
            keyboard = [["📊 تحليل صورة"], ["⚙️ إعدادات التحليل"], ["📈 توصية"], ["الرجوع للقائمة الرئيسية"]]
            
            # تنسيق وقت الصفقة للعرض
            time_display = format_trade_time_for_prompt(trade_time)
            
            # إعداد النص النهائي بدون تكرار
            full_result = (
                f"✅ **تم التحليل بنجاح!**\n"
                f"📈 **نتائج تحليل الشارت:**\n"
                f"━━━━━━━━━━━━━━━━\n"
                f"{result}\n\n"
                f"📊 **الإعدادات المستخدمة:**\n"
                f"• سرعة الشموع: {candle}\n"
                f"• {time_display}\n\n"
                f"━━━━━━━━━━━━━━━━\n"
                f"🤖 **Obeida Trading - نظام التحليل الفني**"
            )
            
            # تنظيف النهائي من التكرارات
            full_result = clean_repeated_text(full_result)
            
            # تقسيم النتيجة إذا كانت طويلة
            if len(full_result) > 4000:
                parts = split_message(full_result, max_length=4000)
                
                # إرسال الجزء الأول مع تعديل الرسالة المنتظرة
                await wait_msg.edit_text(
                    parts[0],
                    parse_mode="Markdown"
                )
                
                # إرسال الأجزاء المتبقية
                for part in parts[1:]:
                    await update.message.reply_text(part, parse_mode="Markdown")
            else:
                await wait_msg.edit_text(
                    full_result,
                    parse_mode="Markdown"
                )
            
            # إرسال الأزرار
            await update.message.reply_text(
                "📊 **اختر الإجراء التالي:**",
                reply_markup=ReplyKeyboardMarkup(keyboard, resize_keyboard=True, one_time_keyboard=False)
            )
        else:
            print(f"Groq Vision API Error: {response.status_code} - {response.text}")
            keyboard = [["📊 تحليل صورة"], ["الرجوع للقائمة الرئيسية"]]
            await wait_msg.edit_text(f"❌ **خطأ في إرسال الصورة:** {response.status_code}")
            
    except requests.exceptions.Timeout:
        await wait_msg.edit_text("⏱️ تجاوز الوقت المحدد إرسال الصورة. حاول مرة أخرى.")
    except Exception as e:
        print(f"خطأ في تحليل الصورة: {e}")
        keyboard = [["📊 تحليل صورة"], ["الرجوع للقائمة الرئيسية"]]
        await wait_msg.edit_text("❌ **حدث خطأ في إرسال الصورة.**\nيرجى التأكد من وضوح الصورة والمحاولة مرة أخرى.")
    finally:
        if os.path.exists(path):
            os.remove(path)
    
    return MAIN_MENU

# --- الدوال الأساسية ---
async def start(update: Update, context: ContextTypes.DEFAULT_TYPE):
    """بدء البوت"""
    keyboard = [
        ["⚙️ إعدادات التحليل", "📊 تحليل صورة"],
        ["💬 دردشة", "📈 توصية"]
    ]
    
    await update.message.reply_text(
        "🚀 **أهلاً بك في Obeida Trading **\n\n"
        "🤖 **المميزات الجديدة:**\n"
        "• تحليل فني متقدم للشارتات\n"
        "• 🆕 دردشة \n"
        "• 📈 نظام توصيات جاهزة\n"
        "• إعدادات تخصيص كاملة\n"
        "اختر أحد الخيارات:",
        reply_markup=ReplyKeyboardMarkup(keyboard, resize_keyboard=True, one_time_keyboard=False),
        parse_mode="Markdown"
    )
    return MAIN_MENU

async def handle_main_menu(update: Update, context: ContextTypes.DEFAULT_TYPE):
    """معالجة اختيارات القائمة الرئيسية"""
    user_message = update.message.text
    user_id = update.effective_user.id
    
    if user_message == "⚙️ إعدادات التحليل":
        keyboard = [CANDLE_SPEEDS[i:i+3] for i in range(0, len(CANDLE_SPEEDS), 3)]
        keyboard.append(["الرجوع للقائمة الرئيسية"])
        
        await update.message.reply_text(
            "⚙️ **إعدادات التحليل الفني**\n\n"
            "حدد سرعة الشموع للبدء:",
            reply_markup=ReplyKeyboardMarkup(keyboard, resize_keyboard=True, one_time_keyboard=False)
        )
        return SETTINGS_CANDLE
    
    elif user_message == "📊 تحليل صورة":
        candle, trade_time = get_user_setting(user_id)
        
        if not candle or not trade_time:
            keyboard = [["⚙️ إعدادات التحليل"], ["الرجوع للقائمة الرئيسية"]]
            await update.message.reply_text(
                "❌ **يجب ضبط الإعدادات أولاً**\n\n"
                "الرجاء ضبط سرعة الشموع ومدة الصفقة قبل التحليل.",
                reply_markup=ReplyKeyboardMarkup(keyboard, resize_keyboard=True, one_time_keyboard=False),
                parse_mode="Markdown"
            )
            return MAIN_MENU
        else:
            keyboard = [["الرجوع للقائمة الرئيسية"]]
            
            time_display = format_trade_time_for_prompt(trade_time)
            
            await update.message.reply_text(
                f"📊 **جاهز للتحليل**\n\n"
                f"الإعدادات الحالية:\n"
                f"• سرعة الشموع: {candle}\n"
                f"• {time_display}\n\n"
                f"أرسل صورة الرسم البياني (الشارت) الآن:",
                reply_markup=ReplyKeyboardMarkup(keyboard, resize_keyboard=True, one_time_keyboard=False),
                parse_mode="Markdown"
            )
            return ANALYZE_MODE
    
    elif user_message == "💬 دردشة":
        return await start_chat_mode(update, context)
    
    elif user_message == "📈 توصية":
        return await start_recommendation_mode(update, context)
    
    keyboard = [["⚙️ إعدادات التحليل", "📊 تحليل صورة"], ["💬 دردشة", "📈 توصية"]]
    await update.message.reply_text(
        "اختر أحد الخيارات من القائمة:",
        reply_markup=ReplyKeyboardMarkup(keyboard, resize_keyboard=True, one_time_keyboard=False)
    )
    return MAIN_MENU

async def handle_settings_candle(update: Update, context: ContextTypes.DEFAULT_TYPE):
    """معالجة اختيار سرعة الشموع"""
    user_message = update.message.text
    user_id = update.effective_user.id
    
    if user_message == "الرجوع للقائمة الرئيسية":
        keyboard = [["⚙️ إعدادات التحليل", "📊 تحليل صورة"], ["💬 دردشة", "📈 توصية"]]
        await update.message.reply_text(
            "🏠 العودة للقائمة الرئيسية",
            reply_markup=ReplyKeyboardMarkup(keyboard, resize_keyboard=True, one_time_keyboard=False)
        )
        return MAIN_MENU
    
    if user_message in CANDLE_SPEEDS:
        save_user_setting(user_id, "candle", user_message)
        
        keyboard = [TRADE_TIMES[i:i+2] for i in range(0, len(TRADE_TIMES), 2)]
        keyboard.append(["الرجوع للقائمة الرئيسية"])
        
        await update.message.reply_text(
            f"✅ **تم تعيين سرعة الشموع:** {user_message}\n\n"
            f"الآن حدد **مدة الصفقة** المتوقعة:\n\n"
            f"📊 **خيارات مدة الصفقة:**\n"
            f"• **قصير (1m-15m)**: تنفيذ سريع، مخاطر منخفضة\n"
            f"• **متوسط (4h-Daily)**: انتظار أيام، مخاطر متوسطة\n"
            f"• **طويل (Weekly-Monthly)**: استثمار طويل، مخاطر مرتفعة\n\n"
            f"اختر الإطار الزمني المناسب لاستراتيجيتك:",
            reply_markup=ReplyKeyboardMarkup(keyboard, resize_keyboard=True, one_time_keyboard=False),
            parse_mode="Markdown"
        )
        return SETTINGS_TIME
    
    await update.message.reply_text("❌ الرجاء اختيار سرعة شموع صحيحة.")
    return SETTINGS_CANDLE

async def handle_settings_time(update: Update, context: ContextTypes.DEFAULT_TYPE):
    """معالجة اختيار مدة الصفقة"""
    user_message = update.message.text
    user_id = update.effective_user.id
    
    if user_message == "الرجوع للقائمة الرئيسية":
        keyboard = [["⚙️ إعدادات التحليل", "📊 تحليل صورة"], ["💬 دردشة", "📈 توصية"]]
        await update.message.reply_text(
            "🏠 العودة للقائمة الرئيسية",
            reply_markup=ReplyKeyboardMarkup(keyboard, resize_keyboard=True, one_time_keyboard=False)
        )
        return MAIN_MENU
    
    if user_message in TRADE_TIMES:
        save_user_setting(user_id, "trade_time", user_message)
        
        keyboard = [["📊 تحليل صورة"], ["💬 دردشة"], ["📈 توصية"], ["الرجوع للقائمة الرئيسية"]]
        
        candle, _ = get_user_setting(user_id)
        
        await update.message.reply_text(
            f"🚀 **تم حفظ الإعدادات بنجاح!**\n\n"
            f"✅ سرعة الشموع: {candle}\n"
            f"✅ مدة الصفقة: {user_message}\n\n"
            f"يمكنك الآن تحليل صورة أو الدردشة:",
            reply_markup=ReplyKeyboardMarkup(keyboard, resize_keyboard=True, one_time_keyboard=False),
            parse_mode="Markdown"
        )
        return MAIN_MENU
    
    await update.message.reply_text("❌ الرجاء اختيار مدة صفقة صحيحة.")
    return SETTINGS_TIME

async def handle_analyze_mode(update: Update, context: ContextTypes.DEFAULT_TYPE):
    """معالجة وضع التحليل"""
    user_message = update.message.text
    user_id = update.effective_user.id
    
    if user_message == "الرجوع للقائمة الرئيسية":
        keyboard = [["⚙️ إعدادات التحليل", "📊 تحليل صورة"], ["💬 دردشة", "📈 توصية"]]
        await update.message.reply_text(
            "🏠 العودة للقائمة الرئيسية",
            reply_markup=ReplyKeyboardMarkup(keyboard, resize_keyboard=True, one_time_keyboard=False)
        )
        return MAIN_MENU
    
    await update.message.reply_text(
        "📤 **الرجاء إرسال صورة الشارت فقط**\nأو اضغط 'الرجوع للقائمة الرئيسية'",
        reply_markup=ReplyKeyboardMarkup([["الرجوع للقائمة الرئيسية"]], resize_keyboard=True, one_time_keyboard=False)
    )
    return ANALYZE_MODE

async def handle_photo_in_analyze_mode(update: Update, context: ContextTypes.DEFAULT_TYPE):
    """معالجة الصور في وضع التحليل"""
    return await handle_photo_analysis(update, context)

async def help_command(update: Update, context: ContextTypes.DEFAULT_TYPE):
    """أمر المساعدة"""
    help_text = """
    🤖 **أوامر البوت:**
    
    /start - بدء البوت والعودة للقائمة الرئيسية
    /help - عرض رسالة المساعدة
    
    ⚙️ **كيفية الاستخدام:**
    1. استخدم أزرار القائمة للتنقل
    2. أرسل صورة الشارت للتحليل
    3. اختر "دردشة" للاستفسارات النصية
    4. اختر "توصية" لتحليل العملات
    
    📅 **أفضل أيام التداول (أيام السيولة الحقيقية):**
     ليست كل أيام الأسبوع متساوية في سوق المال، وإليك الترتيب حسب الأفضلية:

     1️⃣ **الثلاثاء والأربعاء (الأيام الذهبية):** - تشهد أعلى معدلات سيولة وحركة سعرية واضحة (Range) طوال الأسبوع.
     2️⃣ **الخميس:** - ممتاز جداً، وغالباً ما يشهد حركات "انعكاس" أو استكمال لاتجاه الأسبوع.
     3️⃣ **الاثنين:** - يوم "تكوين الاتجاه"، السيولة تكون متوسطة، وغالباً ما يستخدمه المحترفون لمراقبة فخاخ السيولة.

     ⚠️ **تجنب التداول:** - **يوم الجمعة:** (خاصة بعد الظهر) حيث تقوم البنوك بإغلاق مراكزها، مما يسبب حركات عشوائية.
      - **عطلات نهاية الأسبوع (للعملات العادية):** الأسواق مغلقة.

     ⏰ **أفضل ساعات التداول (مناطق القتل):**
      تتضاعف دقة البوت عند استخدامه في هذه الأوقات:
      • **جلسة لندن (08:00 - 11:00 GMT):** بداية السيولة الأوروبية.
      • **تداخل لندن ونيويورك (13:00 - 16:00 GMT):** ذروة السيولة العالمية (أقوى حركة).

    
    ⏱️ **خيارات مدة الصفقة:**
    • **قصير (1m-15m)**: تنفيذ سريع، مخاطر منخفضة
    • **متوسط (4h-Daily)**: انتظار أيام، مخاطر متوسطة
    • **طويل (Weekly-Monthly)**: استثمار طويل، مخاطر مرتفعة
    
    """
    await update.message.reply_text(help_text, parse_mode="Markdown")

async def cancel(update: Update, context: ContextTypes.DEFAULT_TYPE):
    """إلغاء المحادثة"""
    await update.message.reply_text(
        "تم الإلغاء. اكتب /start للبدء من جديد.",
        reply_markup=ReplyKeyboardRemove()
    )
    return ConversationHandler.END

# --- الحل النهائي ---
def run_flask_server():
    """تشغيل Flask server"""
    port = int(os.environ.get('PORT', 8080))
    app.run(host='0.0.0.0', port=port, debug=False, use_reloader=False)

def run_telegram_bot():
    """تشغيل Telegram bot"""
    print("🤖 Starting Telegram Bot...")
    
    # تهيئة قاعدة البيانات
    init_db()
    
    # إنشاء تطبيق Telegram
    application = Application.builder().token(TOKEN).build()
    
    # معالج المحادثة
    conv_handler = ConversationHandler(
        entry_points=[CommandHandler('start', start)],
        states={
            MAIN_MENU: [
                MessageHandler(filters.TEXT & ~filters.COMMAND, handle_main_menu)
            ],
            SETTINGS_CANDLE: [
                MessageHandler(filters.TEXT & ~filters.COMMAND, handle_settings_candle)
            ],
            SETTINGS_TIME: [
                MessageHandler(filters.TEXT & ~filters.COMMAND, handle_settings_time)
            ],
            CHAT_MODE: [
                MessageHandler(filters.TEXT & ~filters.COMMAND, handle_chat_message)
            ],
            ANALYZE_MODE: [
                MessageHandler(filters.TEXT & ~filters.COMMAND, handle_analyze_mode),
                MessageHandler(filters.PHOTO, handle_photo_in_analyze_mode)
            ],
            RECOMMENDATION_MODE: [
                MessageHandler(filters.TEXT & ~filters.COMMAND, handle_recommendation_selection)
            ],
            CATEGORY_SELECTION: [
                MessageHandler(filters.TEXT & ~filters.COMMAND, handle_recommendation_selection)
            ],
        },
        fallbacks=[CommandHandler('start', start), CommandHandler('cancel', cancel)],
        allow_reentry=True
    )
    
    application.add_handler(conv_handler)
    application.add_handler(CommandHandler("help", help_command))
    application.add_handler(CommandHandler("cancel", cancel))
    
    # إضافة معالج للنصوص
    application.add_handler(MessageHandler(filters.TEXT & ~filters.COMMAND, handle_main_menu))
    
    print("✅ Telegram Bot initialized successfully")
    print("📡 Bot is now polling for updates...")
    
    # تشغيل البوت
    application.run_polling(allowed_updates=Update.ALL_TYPES, drop_pending_updates=True)

def main():
    """الدالة الرئيسية"""
    print("🚀 Starting Obeida Trading...")
    
    # تشغيل Flask في thread منفصل
    flask_thread = threading.Thread(target=run_flask_server, daemon=True)
    flask_thread.start()
    
    print(f"🌐 Flask server started on port {os.environ.get('PORT', 8080)}")
    
    # تشغيل Telegram bot في thread الرئيسي
    run_telegram_bot()

if __name__ == "__main__":
    main()
