import logging
import base64
import os
import sqlite3
import re
import requests
import threading
import time
import sys
from telegram import Update, ReplyKeyboardMarkup, KeyboardButton, ReplyKeyboardRemove
from telegram.ext import Application, CommandHandler, MessageHandler, CallbackQueryHandler, filters, ContextTypes, ConversationHandler
from flask import Flask

# --- الإعدادات ---
TOKEN = os.environ.get('TOKEN', "7324911542:AAGcVkwzjtf3wDB3u7cprOLVyoMLA5JCm8U")
GROQ_KEY = os.environ.get('GROQ_KEY', "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")
GROQ_URL = "https://api.groq.com/openai/v1/chat/completions"
GROQ_MODEL = "meta-llama/llama-4-maverick-17b-128e-instruct"
DB_NAME = "abood-gpt.db"

CANDLE_SPEEDS = ["S5", "S10", "S15", "S30", "M1", "M2", "M3", "M5", "M10", "M15", "M30", "H1", "H4", "D1"]
TRADE_TIMES = ["قصير (1m-15m)", "متوسط (4h-Daily)", "طويل (Weekly-Monthly)"]

# توزيع العملات للنظام الجديد
CATEGORIES = {
    "فوركس - عملات رئيسية 💹": [
        "EUR/USD", "GBP/USD", "USD/JPY", "AUD/USD", 
        "USD/CHF", "USD/CAD", "NZD/USD"
    ],
    "فوركس - تقاطعات اليورو 🇪🇺": [
        "EUR/GBP", "EUR/JPY", "EUR/AUD", "EUR/CAD", 
        "EUR/NZD", "EUR/CHF"
    ],
    "فوركس - تقاطعات الباوند 🇬🇧": [
        "GBP/JPY", "GBP/AUD", "GBP/CAD", "GBP/NZD", 
        "GBP/CHF"
    ],
    "عملات ثانوية وأخرى 💱": [
        "AUD/JPY", "AUD/CAD", "AUD/NZD", "CAD/JPY", 
        "NZD/JPY", "CHF/JPY"
    ],
    "عملات غريبة (Exotics) 🌍": [
        "USD/TRY", "USD/ZAR", "USD/MXN", "USD/SGD", 
        "USD/NOK", "USD/SEK"
    ],
    "مؤشرات عالمية 📊": [
        "S&P 500", "Dow Jones (US30)", "DAX 40 (GER40)", 
        "FTSE 100", "CAC 40", "Nikkei 225", "ASX 200", "Hang Seng"
    ],
    "معادن وطاقة 🏗️": [
        "الذهب (XAUUSD)", "الفضة (XAGUSD)", "البلاتين (XPTUSD)", 
        "النحاس (Copper)", "نفط برنت (UKOIL)", "النفط الخام (USOIL)", "الغاز الطبيعي"
    ],
    "ناسداك وتكنولوجيا 🖥️": [
        "NAS100", "US Tech 100", "FANG+"
    ],
    "عملات رقمية ₿": [
        "BTC/USD", "ETH/USD", "SOL/USD", "XRP/USD", 
        "ADA/USD", "DOT/USD", "LTC/USD"
    ]
}


# حالات المحادثة
MAIN_MENU, SETTINGS_CANDLE, SETTINGS_TIME, CHAT_MODE, ANALYZE_MODE, RECOMMENDATION_MODE, CATEGORY_SELECTION = range(7)

# --- Flask Server ---
app = Flask(__name__)

@app.route('/')
def home():
    return """
    <!DOCTYPE html>
    <html>
    <head>
        <title>Obeida Trading</title>
        <style>
            body { font-family: Arial, sans-serif; text-align: center; padding: 50px; }
            h1 { color: #2c3e50; }
            .status { background: #2ecc71; color: white; padding: 10px 20px; border-radius: 5px; display: inline-block; }
        </style>
    </head>
    <body>
        <h1> 📊 Obeida Trading Telegram Bot 📊</h1>
        <p>Chat & Technical Analysis Bot</p>
        <div class="status">✅ Obeida Trading Running</div>
        <p>Last Ping: """ + time.strftime("%Y-%m-%d %H:%M:%S") + """</p>
    </body>
    </html>
    """

@app.route('/health')
def health():
    return {"status": "active", "timestamp": time.time()}

@app.route('/ping')
def ping():
    return "PONG"

# --- قاعدة البيانات ---
def init_db():
    conn = sqlite3.connect(DB_NAME)
    cursor = conn.cursor()
    cursor.execute('''
        CREATE TABLE IF NOT EXISTS users (
            user_id INTEGER PRIMARY KEY, 
            candle TEXT DEFAULT 'M5', 
            trade_time TEXT DEFAULT 'متوسط (4h-Daily)',
            chat_context TEXT DEFAULT ''
        )
    ''')
    cursor.execute('''
        CREATE TABLE IF NOT EXISTS chat_history (
            id INTEGER PRIMARY KEY AUTOINCREMENT,
            user_id INTEGER,
            role TEXT,
            content TEXT,
            timestamp DATETIME DEFAULT CURRENT_TIMESTAMP
        )
    ''')
    conn.commit()
    conn.close()
    print("✅ Database initialized")

def save_user_setting(user_id, col, val):
    conn = sqlite3.connect(DB_NAME)
    cursor = conn.cursor()
    cursor.execute(f"INSERT OR IGNORE INTO users (user_id) VALUES (?)", (user_id,))
    cursor.execute(f"UPDATE users SET {col} = ? WHERE user_id = ?", (val, user_id))
    conn.commit()
    conn.close()

def get_user_setting(user_id):
    conn = sqlite3.connect(DB_NAME)
    cursor = conn.cursor()
    cursor.execute("SELECT candle, trade_time FROM users WHERE user_id = ?", (user_id,))
    res = cursor.fetchone()
    conn.close()
    if res:
        return res
    return ("M5", "متوسط (4h-Daily)")

def format_trade_time_for_prompt(trade_time):
    """تنسيق وقت الصفقة للبرومبت"""
    if trade_time == "قصير (1m-15m)":
        return "مدة الصفقة المتوقعة: قصير الأجل (1 دقيقة إلى 15 دقيقة) - تنفيذ سريع، مخاطر منخفضة"
    elif trade_time == "متوسط (4h-Daily)":
        return "مدة الصفقة المتوقعة: متوسط الأجل (4 ساعات إلى يومي) - انتظار أيام، مخاطر متوسطة"
    elif trade_time == "طويل (Weekly-Monthly)":
        return "مدة الصفقة المتوقعة: طويل الأجل (أسبوعي إلى شهري) - استثمار طويل، مخاطر مرتفعة"
    else:
        return f"مدة الصفقة المتوقعة: {trade_time}"

# --- معالجة الصور بشكل صحيح ---
def encode_image(image_path):
    """تحويل الصورة إلى base64 بشكل صحيح"""
    try:
        with open(image_path, "rb") as image_file:
            # قراءة الملف وتشفيره مباشرة
            encoded_string = base64.b64encode(image_file.read()).decode('utf-8')
        return encoded_string
    except Exception as e:
        print(f"Error encoding image: {e}")
        return None

# --- دوال المساعدة للتعامل مع النصوص ---
def clean_repeated_text(text):
    """تنظيف النص من التكرارات وتحسين التنسيق"""
    if not text:
        return ""
    
    # إزالة أي تنسيق مكرر لـ "نتائج الفحص الفني"
    if "📊 **نتائج الفحص الفني**:" in text:
        # إزالة العناوين المكررة
        text = re.sub(r'(📊 \*\*نتائج الفحص الفني\*\*:[\s\S]*?)(?=📊 \*\*نتائج الفحص الفني\*\*:)', '', text, flags=re.DOTALL)
    
    # إزالة أي قسم "### تحليل الشارت المرفق" إذا كان مكرراً
    if "### تحليل الشارت المرفق" in text:
        # الحفاظ على أول قسم فقط
        sections = text.split("### تحليل الشارت المرفق")
        if len(sections) > 1:
            # أخذ القسم الأول فقط وإضافة العنوان
            text = "### تحليل الشارت المرفق" + sections[1]
    
    # إزالة العناوين المكررة للتحليل
    patterns_to_clean = [
        r'📊\s*\*\*التحليل الفني\*\*:',
        r'🎯\s*\*\*التوصية والتوقعات\*\*:',
        r'⚠️\s*\*\*إدارة المخاطر\*\*:',
        r'📝\s*\*\*ملاحظات التحليل\*\*:'
    ]
    
    for pattern in patterns_to_clean:
        matches = re.findall(pattern, text)
        if len(matches) > 1:
            # استبدال التكرارات
            parts = re.split(pattern, text)
            if len(parts) > 1:
                text = parts[0] + re.search(pattern, text).group() + parts[1]
                for i in range(2, len(parts)):
                    text += parts[i]
    
    # تقسيم النص إلى فقرات وإزالة التكرار
    paragraphs = [p.strip() for p in text.split('\n\n') if p.strip()]
    
    unique_paragraphs = []
    seen_paragraphs = set()
    
    for paragraph in paragraphs:
        # إنشاء مفتاح فريد لكل فقرة (أول 50 حرفاً)
        key = paragraph[:50].strip().lower()
        if key not in seen_paragraphs:
            unique_paragraphs.append(paragraph)
            seen_paragraphs.add(key)
    
    cleaned_text = '\n\n'.join(unique_paragraphs)
    
    # قطع النص إذا كان طويلاً جداً
    if len(cleaned_text) > 2000:
        if '\n\n' in cleaned_text[:2200]:
            cut_point = cleaned_text[:2200].rfind('\n\n')
            cleaned_text = cleaned_text[:cut_point] + "\n\n📋 ...تم اختصار النتيجة"
        else:
            cleaned_text = cleaned_text[:2000] + "...\n\n📋 تم اختصار النتيجة"
    
    return cleaned_text

def split_message(text, max_length=4000):
    """تقسيم الرسالة الطويلة إلى أجزاء"""
    if len(text) <= max_length:
        return [text]
    
    parts = []
    while len(text) > max_length:
        split_point = text[:max_length].rfind('\n\n')
        if split_point == -1:
            split_point = text[:max_length].rfind('\n')
        if split_point == -1:
            split_point = max_length - 100
        
        parts.append(text[:split_point])
        text = text[split_point:].lstrip()
    
    if text:
        parts.append(text)
    
    return parts

# --- وظائف نظام التوصية الجديد ---
def get_groq_analysis(symbol):
    """الحصول على تحليل من Groq API للعملة"""
    headers = {
        "Authorization": f"Bearer {GROQ_KEY}",
        "Content-Type": "application/json"
    }
    
    prompt = f"""
    بصفتك محللاً مالياً وخبيراً في استراتيجيات التداول الكمي والتقني، قم بإجراء تحليل معمق لعملة {symbol} وفق بروتوكول "تلاقي الأدلة" (Confluence Analysis):

⚠️ **الطبقة الأولى: صمام الأمان المطلق (ULTIMATE_PROTOCOL_ZERO) - الإلغاء الفوري عند فقدان أي شرط:**

1. **حتمية الاستنفاذ (Exhaustion Imperative)**: إذا كانت آخر شمعة ذات جسم كبير (Effort) فشلت في إغلاق أعلى/أدنى من الهيكل الرئيسي، يُعتبر السوق في حالة "إرهاق نهائي" والصفقة ممنوعة.

2. **توافق الفركتلات الزمنية (Temporal Fractal Alignment)**: يجب أن يكون هناك تطابق بين حركة الفريم الحالي {candle} وكل من الفريم الأعلى والأدنى على الأقل في 3 نقاط رئيسية، وإلا فهي "حركة وهمية".

3. **سيولة المغناطيس المزدوج (Dual Magnetic Liquidity)**: يُلغى الدخول إذا لم نر سحباً للسيولة (Sweep) في الاتجاه المعاكس يتبعه تشكيل فخ (Inducement) ثم ارتداد قوي مع ترك فجوة (FVG).

4. **نقطة التوازن الكونية (Cosmic Equilibrium Point - CEP)**: يجب أن يكون الدخول فقط على مستويات التصحيح العميقة 0.618-0.786-0.886 من آخر موجة مع تطابقها مع منطقة التراكم (Accumulation Zone).

5. **قاعدة الثبات الثنائي (Binary Stability Rule)**: يشترط إغلاق شمعتين متتاليتين بجسم كامل خارج منطقة التذبذب الأساسية مع تضاعف الحجم في الشمعة الثانية.

🔬 **الطبقة الثانية: النظام التحليلي المتقدم (21 مرحلة - Advanced Analytical Matrix):**

[قسم البصمة الرقمية والبنية الخفية]
1. **التدقيق الرقمي الكمي (Quantum Digital Audit)**: فحص دقة كل رقم في الشارت مع تطبيق معادلة التطابق: السعر الحالي ÷ 1.618 يجب أن يطابق نقطة محورية رئيسية.

2. **رسم الهيكل الخفي (Invisible Structure Mapping)**: تحديد الاتجاه العام ورسم القمم والقيعان الوهمية (التي تشير إليها الذيول دون أن يلمسها السعر).

3. **مواءمة الفركتلات المتعددة (Multi-Fractal Alignment)**: التأكد من توافق حركة 5 فريمات مختلفة معاً (D1 → H4 → H1 → 30M → 15M).

4. **قصة السوق الكاملة (Full Market Narrative)**: تحديد المرحلة الحالية بدقة: (التجميع الأولي - الاختبار - إعادة التجميع - التوزيع - الهبوط - التراكم النهائي).

[قسم تشريح السيولة الذكية]
5. **خرائط السيولة الكمومية (Quantum Liquidity Maps)**: رسم EQL (سيولة القمم المتساوية) و EQH (سيولة القيعان المتساوية) مع السيولة الخارجية البعيدة.

6. **رصد مناطق التضليل (Deception Zone Detection)**: تحديد مناطق الإغراء الثلاثية (Inducement Triad) التي يستخدمها البنك المركزي لجذب التجزئة.

7. **مرشح التلاعب المتقدم (Advanced Manipulation Filter)**: التمييز بين الكسر الحقيقي (BOS) والسحب الوهمي باستخدام 5 مؤشرات: حجم التداول + RSI + MACD + التيول + سرعة الحركة.

8. **تحليل الجهد مقابل النتائج المتعددة (Multi-Effort vs Result)**: مقارنة 3 شموع متتالية من حيث الحجم والجسم والتيول لتحديد نية السوق الحقيقية.

[قسم الهندسة السعرية المتطورة]
9. **مناطق العرض/الطلب الديناميكية (Dynamic S/D Zones)**: بناء مناطق تعتمد على الكثافة وليس فقط ردود الفعل السابقة مع إعطاء وزن لكل منطقة.

10. **تحقق من كتل الأوامر الذكية (Smart Order Block Validation)**: تحديد الـ Order Blocks النشطة بناءً على 3 معايير: حجم السيولة + وقت التشكيل + سرعة رد الفعل.

11. **شبكة الفجوات السعرية (FVG Network Analysis)**: رصد الفجوات المتداخلة بين الفريمات المختلفة وتحديد أيها سيملأ أولاً.

12. **مصفوفة الخصم/البريميوم (Premium/Discount Matrix)**: تحديد مناطق الـ OTE المثالية مع إضافة عامل الوقت (Time Factor) لكل مستوى.

[قسم المؤشرات الكمية المتقدمة]
13. **مجموعة المؤشرات الكمية (Quantitative Indicators Suite)**:
    - **RSI Divergence Hunter**: البحث عن 3 أنواع من الدايفرجنس (Reg, Hidden, Exhaustion)
    - **MACD Wave Analyzer**: تحليل موجات MACD وتقاطعاتها مع خط الإشارة
    - **Volume Profile Scanner**: تحليل توزيع الحجم عند مستويات السعر المختلفة
    - **VWAP + Bollinger Bands Fusion**: تقاطع VWAP مع نطاقات بولينجر
    - **Fibonacci Time Zones**: تطبيق مستويات فيبوناتشي الزمنية مع السعرية
    - **Market Structure Break Detector**: كاشف كسر الهياكل مع تأكيدات متعددة

[قسم مصفوفة التنفيذ الذكي]
14. **مصفوفة الدخول المثلى (Optimal Entry Matrix)**: تتطلب تلاقي 4 عناصر: OB + FVG + Sweep + Time Fib Level.

15. **النمذجة الزمنية الكمية (Quantitative Temporal Modeling)**: حساب الوقت المطلوب للوصول لكل هدف باستخدام معادلة: (المسافة ÷ متوسط السرعة التاريخية) × عامل التصحيح.

16. **مفتاح القتل الذكي (Smart Kill Switch)**: تحديد 3 نقاط إلغاء: فنية + زمنية + حجمية.

17. **تحليل التشفير للذيول (Wick Cryptanalysis Pro)**: دراسة التيول باستخدام نظرية المعلومات لتحديد نية المؤسسات.

[قسم إدارة المخاطر المتطورة]
18. **مصفوفة المخاطر الكمية (Quantitative Risk Matrix)**:
    - نسبة العائد/المخاطرة: لا تقل عن 1:3
    - أقصى مخاطرة: 2% من رأس المال
    - تقييم احتمالية النجاح: بناءً على 7 عوامل

19. **نظام الحماية المتدرج (Tiered Protection System)**:
    - **Stop Loss الأساسي**: خلف أقوى منطقة دعم/مقاومة
    - **Stop Loss الديناميكي**: يتحرك مع السعر بعد تحقيق TP1
    - **Stop Loss الزمني**: إذا لم يتحرك السعر لصالحك خلال X شموع

20. **تحليل السياق الاقتصادي المصغر (Micro Economic Context)**:
    - تأثير الأخبار على المدى القريب
    - أوقات التداول النشطة
    - ساعات السيولة العالية

المعطيات التقنية:
- إطار الشمعة : (Timeframe): {candle}
- وقت التداول المحدد : {time_for_prompt}

قدم التحليل باللغة العربية وبالتنسيق التالي حصراً (بدون تكرار للمعلومات):

📊 **تقرير الفحص الفني لعملة {symbol}****:
- **البصمة الزمنية**: (داخل/خارج منطقة القتل السعري - Kill Zone)
- **حالة الهيكل**: (صاعد/هابط) + (مرحلة وايكوف الحالية)
- **خريطة السيولة**: (أقرب فخ سيولة Inducement + مناطق السيولة المستهدفة)
- **الفجوات السعرية (FVG)**: (المناطق التي سيعود السعر لتغطيتها)

🎯 **الإشارة التنفيذية**:
- **السعر الحالي**: [اذكر السعر الدقيق الذي تراه الآن للتأكد من دقة القراءة]
- **القرار الفني**: (شراء 🟢 / بيع 🔴)
- **قوة الإشارة 🔰**: (🔥 عالية - تلاقي 3 مدارس / ⚡ متوسطة / ❄️ ضعيفة)
- **نقطة الدخول (Entry)**: [ السعر الدقيق بناءً على الـ Order Block ]
- **الأهداف الربحية (TPs)**:
    - 🎯 **TP1**: [سحب أول سيولة داخلية]
    - 🎯 **TP2**: [الهدف الرئيسي - منطقة عرض/طلب قوية]
    - 🎯 **TP3**: [استهداف السيولة الخارجية (Major SSL/BSL) أو سد فجوة سعرية على فريم أكبر]
- **وقف الخسارة (SL)**: [السعر مع 3 طبقات حماية]
- **المدة المتوقعة 🕧**: [عدد الشموع للوصول للهدف TP1 قم بتحويلها الى دقائق كل شمعة 1 دقيقة ]

⚠️ **إدارة المخاطر**:
- **مستوى الثقة**: [% مع ذكر عدد التاكيدات]
- **نقطة الإلغاء**: [السعر الذي يفسد التحليل]

    """
    
    body = {
        "model": GROQ_MODEL,
        "messages": [{"role": "user", "content": prompt}],
        "temperature": 0.1,
        "max_tokens": 1500
    }

    try:
        response = requests.post(GROQ_URL, json=body, headers=headers, timeout=25)
        response.raise_for_status()
        return response.json()['choices'][0]['message']['content'].strip()
    except Exception as e:
        print(f"Error in get_groq_analysis: {e}")
        return "⚠️ حدث خطأ في الاتصال بالمحلل."

async def start_recommendation_mode(update: Update, context: ContextTypes.DEFAULT_TYPE):
    """بدء وضع التوصية"""
    reply_keyboard = [[key] for key in CATEGORIES.keys()]
    reply_keyboard.append(["الرجوع للقائمة الرئيسية"])
    
    await update.message.reply_text(
        "🚀 **نظام التوصيات **\n\n"
        "اختر القسم المطلوب من الأزرار:",
        reply_markup=ReplyKeyboardMarkup(reply_keyboard, resize_keyboard=True)
    )
    return RECOMMENDATION_MODE

async def handle_recommendation_selection(update: Update, context: ContextTypes.DEFAULT_TYPE):
    """معالجة اختيارات نظام التوصية"""
    user_text = update.message.text.strip()
    
    # العودة للقائمة الرئيسية
    if user_text == "الرجوع للقائمة الرئيسية":
        keyboard = [["⚙️ إعدادات التحليل", "📊 تحليل صورة"], ["💬 دردشة", "📈 توصية"]]
        await update.message.reply_text(
            "🏠 العودة للقائمة الرئيسية",
            reply_markup=ReplyKeyboardMarkup(keyboard, resize_keyboard=True, one_time_keyboard=False)
        )
        return MAIN_MENU
    
    # التحقق من الأقسام الرئيسية
    if user_text in CATEGORIES:
        keyboard = [[asset] for asset in CATEGORIES[user_text]]
        keyboard.append(["🔙 العودة للقائمة", "الرجوع للقائمة الرئيسية"])
        
        await update.message.reply_text(
            f"📍 قسم: {user_text}\nاختر العملة الآن:",
            reply_markup=ReplyKeyboardMarkup(keyboard, resize_keyboard=True)
        )
        return CATEGORY_SELECTION
    
    # التحقق من العملة المختارة
    symbol_to_analyze = None
    for category_list in CATEGORIES.values():
        if user_text in category_list:
            symbol_to_analyze = user_text
            break
    
    # إذا وجدت العملة، ابدأ التحليل
    if symbol_to_analyze:
        wait_msg = await update.message.reply_text(f"⏳ جاري إرسال توصيات `{symbol_to_analyze}`...")
        analysis = get_groq_analysis(symbol_to_analyze)
        
        final_msg = (
            f"📈 **نتائج توصية {symbol_to_analyze}**\n"
            f"━━━━━━━━━━━━━━━━━━\n"
            f"{analysis}\n"
            f"━━━━━━━━━━━━━━━━━━\n"
            f"🤖 **Obeida Trading - نظام التوصيات**"
        )
        
        # تنظيف النص من التكرارات
        final_msg = clean_repeated_text(final_msg)
        
        await wait_msg.edit_text(
            final_msg,
            parse_mode="Markdown"
        )
        
        # عرض الأزرار للاستمرار
        reply_keyboard = [[key] for key in CATEGORIES.keys()]
        reply_keyboard.append(["الرجوع للقائمة الرئيسية"])
        
        await update.message.reply_text(
            "🔽 **اختر قسم آخر أو العودة للقائمة الرئيسية:**",
            reply_markup=ReplyKeyboardMarkup(reply_keyboard, resize_keyboard=True)
        )
        return RECOMMENDATION_MODE
    
    # إذا كان النص "🔙 العودة للقائمة"
    if user_text == "🔙 العودة للقائمة":
        reply_keyboard = [[key] for key in CATEGORIES.keys()]
        reply_keyboard.append(["الرجوع للقائمة الرئيسية"])
        
        await update.message.reply_text(
            "🔙 **العودة للقائمة الرئيسية للتوصيات**\nاختر القسم المطلوب:",
            reply_markup=ReplyKeyboardMarkup(reply_keyboard, resize_keyboard=True)
        )
        return RECOMMENDATION_MODE
    
    # إذا لم يطابق النص أي شيء
    await update.message.reply_text(
        "❌ خيار غير موجود. يرجى اختيار عملة من القائمة الظاهرة في الأزرار.\n\n"
        "اضغط 'الرجوع للقائمة الرئيسية' للعودة.",
        reply_markup=ReplyKeyboardMarkup([["الرجوع للقائمة الرئيسية"]], resize_keyboard=True)
    )
    return RECOMMENDATION_MODE

# --- 🚀 برومبت قوي للدردشة ---
async def start_chat_mode(update: Update, context: ContextTypes.DEFAULT_TYPE):
    """بدء وضع الدردشة المتقدم"""
    keyboard = [
        ["🚀 مساعد شامل", "💼 استشارات احترافية"],
        ["📈 تحليل استثماري", "👨‍💻 دعم برمجي"],
        ["📝 كتابة إبداعية", "🧠 حلول ذكية"],
        ["ايقاف الدردشة", "الرجوع للقائمة الرئيسية"]
    ]
    
    await context.bot.send_message(
        chat_id=update.effective_chat.id,
        text="🚀 **وضع الدردشة Obeida Trading**\n\n"
             "أنا مساعدك الذكي متعدد المواهب:\n"
             "• مستشار استثماري وتحليلات مالية\n"
             "• خبير برمجي وتقني\n"
             "• محلل بيانات واستراتيجيات\n"
             "• كاتب محتوى إبداعي\n"
             "• مساعد شخصي ذكي\n\n"
             "اختر مجال المساعدة أو أرسل سؤالك مباشرة:",
        reply_markup=ReplyKeyboardMarkup(keyboard, resize_keyboard=True, one_time_keyboard=False),
        parse_mode="Markdown"
    )
    return CHAT_MODE

async def handle_chat_message(update: Update, context: ContextTypes.DEFAULT_TYPE):
    """معالجة رسائل الدردشة مع برومبت قوي"""
    user_message = update.message.text
    user_id = update.effective_user.id
    
    # التحقق من الأوامر الخاصة
    if user_message == "ايقاف الدردشة":
        main_keyboard = [["⚙️ إعدادات التحليل", "📊 تحليل صورة"], ["💬 دردشة", "📈 توصية"]]
        await update.message.reply_text(
            "✅ تم إنهاء وضع الدردشة.",
            reply_markup=ReplyKeyboardMarkup(main_keyboard, resize_keyboard=True, one_time_keyboard=False)
        )
        return MAIN_MENU
    
    elif user_message == "الرجوع للقائمة الرئيسية":
        main_keyboard = [["⚙️ إعدادات التحليل", "📊 تحليل صورة"], ["💬 دردشة", "📈 توصية"]]
        await update.message.reply_text(
            "🏠 العودة للقائمة الرئيسية",
            reply_markup=ReplyKeyboardMarkup(main_keyboard, resize_keyboard=True, one_time_keyboard=False)
        )
        return MAIN_MENU
    
    # برومبتات متخصصة حسب الاختيار
    system_prompts = {
        "🚀 مساعد شامل": """أنت Obeida Trading، مساعد ذكي شامل يمتلك معرفة عميقة في:
🎯 **التحليل الفني والمالي:** خبرة في أسواق المال، تحليل الشارتات، واستراتيجيات التداول
💻 **البرمجة والتقنية:** إتقان Python، JavaScript، تطوير الويب، الذكاء الاصطناعي
📊 **البيانات والتحليل:** تحليل البيانات، الإحصاء، وتقديم رؤى استراتيجية
✍️ **الكتابة والإبداع:** صياغة المحتوى، التقارير، والمواد الإعلامية
🧠 **التفكير النقدي:** حل المشكلات المعقدة، التحليل المنطقي، واتخاذ القرارات

**مبادئك الأساسية:**
1. **الدقة أولاً:** معلومات موثوقة ومدروسة
2. **التنظيم:** هيكل واضح مع عناوين ونقاط
3. **القيمة المضافة:** تقديم نصائح إضافية غير مطلوبة
4. **الوضوح:** شرح المفاهيم المعقدة ببساطة
5. **الإبداع:** حلول مبتكرة للمشكلات

**تنسيق الإجابة المثالي:**
🎯 **الجوهر:** (ملخص سريع)
📋 **التفاصيل:** (نقاط مرتبة)
💡 **الإثراء:** (معلومات إضافية مفيدة)
🚀 **التطبيق:** (خطوات عملية)

استخدم اللغة العربية بطلاقة مع لمسة عصرية وجذابة.""",

        "💼 استشارات احترافية": """أنت Obeida Trading، مستشار احترافي في:
📈 **الاستشارات المالية:** تحليل الأسواق، تقييم المخاطر، استراتيجيات الاستثمار
👔 **التخطيط الاستراتيجي:** تحليل SWOT، وضع الأهداف، متابعة الأداء
🤝 **العلاقات المهنية:** التواصل الفعال، التفاوض، بناء الشبكات
📋 **إدارة المشاريع:** التخطيط، التنفيذ، المتابعة، التقييم

**التزاماتك المهنية:**
• الموضوعية والشفافية
• احترام السرية المهنية
• التطوير المستمر
• الالتزام بالأخلاقيات المهنية
• التركيز على النتائج العملية""",

        "📈 تحليل استثماري": """أنت Obeida Trading، محلل استثماري متخصص في:
📊 **التحليل الفني:** قراءة الشارتات، المؤشرات الفنية، أنماط التداول
📉 **التحليل الأساسي:** الأرباح، القوائم المالية، المؤشرات الاقتصادية
🎯 **إدارة المخاطر:** تحديد المخاطر، التحوط، موازنة المحفظة
🔍 **البحث والتنقيب:** فرص الاستثمار، اتجاهات السوق، التنبؤات

**قواعد التحليل:**
• اعتماد البيانات الرسمية والموثوقة
• تحليل متعدد الأبعاد
• مراعاة السياق الاقتصادي
• التوازن بين العائد والمخاطرة
• الشفافية في الافتراضات""",

        "👨‍💻 دعم برمجي": """أنت Obeida Trading، مبرمج خبير ودعم تقني في:
🐍 **Python:** تطبيقات الويب، الذكاء الاصطناعي، تحليل البيانات
🌐 **تطوير الويب:** Frontend, Backend, APIs, Databases
🤖 **الذكاء الاصطناعي:** Machine Learning, NLP, Computer Vision
🛠️ **حل المشكلات:** Debugging, Optimization, Best Practices

**أسلوب العمل:**
• كتابة أكواد نظيفة وموثوقة
• شرح المفاهيم البرمجية بوضوح
• تقديم حلول عملية وفعالة
• تعليم أفضل الممارسات
• دعم التعلم المستمر""",

        "📝 كتابة إبداعية": """أنت Obeida Trading، كاتب إبداعي محترف في:
📄 **المحتوى التقني:** تقارير، أبحاث، مستندات فنية
🎨 **المحتوى التسويقي:** إعلانات، حملات، محتوى وسائل التواصل
📚 **المحتوى التعليمي:** شروحات، دورات، مواد تعليمية
✒️ **الكتابة الإبداعية:** قصص، مقالات، محتوى ممتع

**مبادئ الكتابة:**
• لغة عربية سليمة وجذابة
• تنظيم منطقي وسهل المتابعة
• تكييف الأسلوب حسب الجمهور
• الإبداع مع الحفاظ على الدقة
• جذب الانتباه والإقناع"""
    }
    
    # تحديد البرومبت المناسب
    selected_prompt = system_prompts.get(user_message, """أنت Obeida Trading، مساعد ذكي شامل يمتلك مزيجاً فريداً من:
🧠 **الذكاء العميق:** فهم شامل لمجالات متعددة
🎯 **الدقة الشديدة:** معلومات موثوقة ومدروسة بدقة
🚀 **الإبداع العملي:** حلول مبتكرة وقابلة للتطبيق
💡 **البصيرة الاستراتيجية:** رؤية أعمق من السؤال المطروح

**شخصيتك المميزة:**
- ذكي، صبور، ومتحمس للمعرفة
- تتحدث بلغة عربية فصيحة مع لمسة عصرية
- تحب التفاصيل ولكن تقدمها بشكل منظم
- دائماً تبحث عن "القيمة المخفية" في كل سؤال

**قواعدك الأساسية:**
1. **لا تقل أبداً "لا أعرف"** - ابحث عن أفضل إجابة ممكنة
2. **كن منظماً بشكل ممتاز** - استخدم التبويب والعناوين المناسبة
3. **فكر في ما وراء السؤال** - قدم نصائح إضافية غير متوقعة
4. **ادعم بأمثلة عملية** - اجعل الإجابة قابلة للتطبيق
5. **حفز الفضول** - أضف معلومة تشجع على البحث أكثر

**هيكل الإجابة الأمثل:**
🎯 **اللب:** (تلخيص مركز في جملة واحدة)
📊 **التفاصيل المنظمة:** (نقاط مرتبة ومنطقية)
💎 **القيمة المضافة:** (معلومات إضافية ذكية)
🚀 **الخطوة التالية:** (اقتراح عملي للتنفيذ)

**تذكر جيداً:** أنت Obeida Trading، المساعد الذكي الذي يحول التعقيد إلى بساطة، ويمنحك دائماً أكثر مما تطلب!""")
    
    # إذا كان اختياراً من القائمة، اطلب التفاصيل
    if user_message in system_prompts:
        await update.message.reply_text(
            f"✅ **تم اختيار: {user_message}**\n\n"
            f"🎯 **جاهز لخدمتك في هذا التخصص**\n"
            f"أرسل سؤالك الآن وسأقدم لك إجابة متخصصة وشاملة:",
            parse_mode="Markdown"
        )
        return CHAT_MODE
    
    # إظهار حالة المعالجة
    wait_msg = await update.message.reply_text("Obeida Trading 🤔...")
    
    try:
        # استدعاء واجهة Groq
        payload = {
            "model": GROQ_MODEL,
            "messages": [
                {"role": "system", "content": selected_prompt},
                {"role": "user", "content": user_message}
            ],
            "max_tokens": 1200,
            "temperature": 0.7
        }
        
        headers = {
            "Authorization": f"Bearer {GROQ_KEY}",
            "Content-Type": "application/json"
        }
        
        response = requests.post(GROQ_URL, headers=headers, json=payload, timeout=60)
        
        if response.status_code == 200:
            result = response.json()['choices'][0]['message']['content']
            
            # تنظيف النص من التكرارات
            result = clean_repeated_text(result)
            
            # إضافة تذييل مميز
            footer = "\n\n━━━━━━━━━━━━━━━━━━\n🤖 **Obeida Trading** - المساعد الذكي "
            result = result + footer
            
            # أزرار الدردشة المتقدمة
            chat_keyboard = [
                ["🚀 مساعد شامل", "💼 استشارات احترافية"],
                ["📈 تحليل استثماري", "👨‍💻 دعم برمجي"],
                ["📝 كتابة إبداعية", "🧠 حلول ذكية"],
                ["ايقاف الدردشة", "الرجوع للقائمة الرئيسية"]
            ]
            
            # تقسيم الرسالة الطويلة
            if len(result) > 4000:
                parts = split_message(result, max_length=4000)
                for i, part in enumerate(parts):
                    if i == 0:
                        await wait_msg.edit_text(
                            f"Obeida Trading 💬\n\n{part}",
                            parse_mode="Markdown"
                        )
                    else:
                        await update.message.reply_text(part, parse_mode="Markdown")
            else:
                await wait_msg.edit_text(
                    f"Obeida Trading 💬\n\n{result}",
                    parse_mode="Markdown"
                )
            
            # إرسال الأزرار بعد الرد
            await update.message.reply_text(
                "🔽 **اختر مجالاً آخر أو اطرح سؤالاً جديداً:**",
                reply_markup=ReplyKeyboardMarkup(chat_keyboard, resize_keyboard=True, one_time_keyboard=False)
            )
            
        else:
            print(f"Groq API Error: {response.status_code} - {response.text}")
            await wait_msg.edit_text(f"❌ حدث خطأ تقني. الرمز: {response.status_code}\nيرجى المحاولة مرة أخرى.")
    
    except requests.exceptions.Timeout:
        await wait_msg.edit_text("⏱️ تجاوز الوقت المحدد. السؤال يحتاج تفكيراً أعمق!\nيمكنك إعادة صياغة السؤال بشكل أوضح.")
    except requests.exceptions.RequestException as e:
        print(f"Network error in chat: {e}")
        await wait_msg.edit_text("🌐 خطأ في الاتصال. تأكد من اتصالك بالإنترنت وحاول مرة أخرى.")
    except Exception as e:
        print(f"خطأ في الدردشة: {e}")
        await wait_msg.edit_text("❌ حدث خطأ غير متوقع. النظام يعمل على الإصلاح تلقائياً...")
    
    return CHAT_MODE

# --- كود تحليل الصور ---
async def handle_photo_analysis(update: Update, context: ContextTypes.DEFAULT_TYPE):
    """معالجة الصور للتحليل الفني"""
    user_id = update.effective_user.id
    candle, trade_time = get_user_setting(user_id)
    
    if not candle or not trade_time:
        keyboard = [["⚙️ إعدادات التحليل"], ["الرجوع للقائمة الرئيسية"]]
        await update.message.reply_text(
            "❌ **يجب ضبط الإعدادات أولاً**\n\n"
            "الرجاء استخدام أزرار القائمة لضبط الإعدادات قبل تحليل الصور.",
            reply_markup=ReplyKeyboardMarkup(keyboard, resize_keyboard=True, one_time_keyboard=False),
            parse_mode="Markdown"
        )
        return MAIN_MENU

    wait_msg = await update.message.reply_text("جاري تحليل صورة 📊...")
    photo = await update.message.photo[-1].get_file()
    path = f"img_{user_id}.jpg"
    await photo.download_to_drive(path)

    try:
        base64_img = encode_image(path)
        
        if not base64_img:
            await wait_msg.edit_text("❌ **خطأ في قراءة الصورة.** يرجى إرسال صورة واضحة.")
            if os.path.exists(path):
                os.remove(path)
            return MAIN_MENU
        
        # تنسيق وقت الصفقة للبرومبت
        time_for_prompt = format_trade_time_for_prompt(trade_time)
        
        # برومبت آمن للتحليل الفني - تم تعديله لمنع التكرار
        prompt = f"""[SYSTEM_TASK: TOTAL_MARKET_DECRYPTION_V12_ULTIMATE_PROTOCOL]
بصفتك "المصفاة الذهبية" لتحليل الصفقات الاحترافية وتوقع تحركات السوق قبل حدوثها، قم بتطبيق بروتوكول التشفير الثلاثي (Triple-Layer Encryption) على الشارت المرفق:

⚠️ **ملاحظة حيوية**: هذا التحليل مبني على لقطة زمنية ثابتة. يجب عليك:
1. التحقق من السعر الحي الحالي
2. مقارنته مع "السعر المرجعي" المذكور
3. التأكد من عدم وجود أخبار عاجلة
4. فقط بعد ذلك تنفذ الصفقة

🔰** اليك بعض القوانين يجب الالتزام بها وهي 🔰** :
1. تحليل شامل للصورة
2. تحديد الأنماط الفنية الظاهرة
3. تقييم قوة الاتجاه
4. تقديم توقع واضح
5. تحليل ذكي للصورة 
6. توقعات ناحجة جدآ 
7. قدم إجابات دقيقة وموضوعية تعتمد على الحقائق والبيانات المتاحة 
8. لا تقدم نسب مخاطرة وهمية ولا توقعات مضمونة.
9. كن واقعياً وموضوعياً في جميع إجاباتك.
10. توقعات ناحجة و رسمية بدون اي إجابات سريعة أو وهمية
11. انتا ذكي جدآ وتوقعات مضمونة و صحيحة 100٪
12. اجعل كل شئ بالغة العربية.
13. اختصار الإجابة بدقة و وضوح و صحة بيانات
14. **شرط التحديث الحي**: إذا مرت أكثر من 3 شموع على الفريم الحالي منذ التقاط الصورة ← يحتاج تأكيد إضافي.
15. **قاعدة الوفرة المعلوماتية**: لا تصدر إشارة إلا إذا توفرت بيانات كافية من 3 مصادر على الأقل (سعر، حجم، زمن).
16. **مرشح التضخم الحجمي**: يُرفض الدخول إذا كان ارتفاع الحجم في الشمعة الأخيرة مفاجئاً وغير مسبوق (أكثر من 300% من المتوسط) دون سبب فني واضح.

⚠️ **الطبقة الأولى: صمام الأمان المطلق (ULTIMATE_PROTOCOL_ZERO) - الإلغاء الفوري عند فقدان أي شرط:**

1. **حتمية الاستنفاذ**: إذا كانت آخر شمعة ذات جسم كبير (Effort) فشلت في إغلاق أعلى/أدنى من الهيكل الرئيسي، يُعتبر السوق في حالة "إرهاق نهائي" والصفقة ممنوعة.

2. **توافق الفركتلات الزمنية**: يجب أن يكون هناك تطابق بين حركة الفريم الحالي {candle} وكل من الفريم الأعلى والأدنى على الأقل في 3 نقاط رئيسية، وإلا فهي "حركة وهمية".

3. **سيولة المغناطيس المزدوج**: يُلغى الدخول إذا لم نر سحباً للسيولة (Sweep) في الاتجاه المعاكس يتبعه تشكيل فخ (Inducement) ثم ارتداد قوي مع ترك فجوة (FVG).

4. **نقطة التوازن الكونية**: يجب أن يكون الدخول فقط على مستويات التصحيح العميقة 0.618-0.786-0.886 من آخر موجة مع تطابقها مع منطقة التراكم (Accumulation Zone).

5. **قاعدة الثبات الثنائي**: يشترط إغلاق شمعتين متتاليتين بجسم كامل خارج منطقة التذبذب الأساسية مع تضاعف الحجم في الشمعة الثانية.

6. **مرشح الحجم الذكي**: رفض أي صفقة إذا كان حجم الشمعة الأخيرة أكبر بثلاثة أضعاف من متوسط الحجم دون سبب منطقي.

🔬 **الطبقة الثانية: النظام التحليلي المتقدم (21 مرحلة - Advanced Analytical Matrix):**

[قسم البصمة الرقمية والبنية الخفية]
1. **التدقيق الرقمي الكمي**: فحص دقة كل رقم في الشارت مع تطبيق معادلة التطابق: السعر الحالي ÷ 1.618 يجب أن يطابق نقطة محورية رئيسية.

2. **رسم الهيكل الخفي**: تحديد الاتجاه العام ورسم القمم والقيعان الوهمية (التي تشير إليها الذيول دون أن يلمسها السعر).

3. **مواءمة الفركتلات المتعددة (Multi-Fractal Alignment)**: 
   - **الفريم الرئيسي**: {candle} (اتجاه + هيكل)
   - **فريم أعلى واحد**: للتأكيد (D1 أو H4)
   - **فريم أدنى واحد أو اثنين**: للدخول الدقيق (30M و/أو 15M)
   - **شرط التوافق**: تطابق الاتجاه العام + تقارب مناطق S/D الرئيسية في 3 فريمات على الأقل

4. **قصة السوق الكاملة**: تحديد المرحلة الحالية بدقة: (التجميع الأولي - الاختبار - إعادة التجميع - التوزيع - الهبوط - التراكم النهائي).

[قسم تشريح السيولة الذكية]
5. **خرائط السيولة الكمومية**: رسم EQL (سيولة القمم المتساوية) و EQH (سيولة القيعان المتساوية) مع السيولة الخارجية البعيدة.

6. **رصد مناطق التضليل**: تحديد مناطق الإغراء الثلاثية (Inducement Triad) التي يستخدمها البنك المركزي لجذب التجزئة.

7. **مرشح التلاعب المتقدم**: التمييز بين الكسر الحقيقي (BOS) والسحب الوهمي باستخدام 5 مؤشرات: حجم التداول + RSI + MACD + التيول + سرعة الحركة.

8. **تحليل الجهد مقابل النتائج المتعددة**: مقارنة 3 شموع متتالية من حيث الحجم والجسم والتيول لتحديد نية السوق الحقيقية.

[قسم الهندسة السعرية المتطورة]
9. **مناطق العرض/الطلب الديناميكية**: بناء مناطق تعتمد على الكثافة وليس فقط ردود الفعل السابقة مع إعطاء وزن لكل منطقة.

10. **تحقق من كتل الأوامر الذكية**: تحديد الـ Order Blocks النشطة بناءً على 3 معايير: حجم السيولة + وقت التشكيل + سرعة رد الفعل.

11. **شبكة الفجوات السعرية**: رصد الفجوات المتداخلة بين الفريمات المختلفة وتحديد أيها سيملأ أولاً.

12. **مصفوفة الخصم/البريميوم**: تحديد مناطق الـ OTE المثالية مع إضافة عامل الوقت (Time Factor) لكل مستوى.

[قسم المؤشرات الكمية المتقدمة]
13. **مجموعة المؤشرات الكمية**:
    - **RSI Divergence Hunter**: البحث عن 3 أنواع من الدايفرجنس (Reg, Hidden, Exhaustion)
    - **MACD Wave Analyzer**: تحليل موجات MACD وتقاطعاتها مع خط الإشارة
    - **Volume Profile Scanner**: تحليل توزيع الحجم عند مستويات السعر المختلفة
    - **VWAP + Bollinger Bands Fusion**: تقاطع VWAP مع نطاقات بولينجر
    - **Fibonacci Time Zones**: تطبيق مستويات فيبوناتشي الزمنية مع السعرية
    - **Market Structure Break Detector**: كاشف كسر الهياكل مع تأكيدات متعددة

[قسم مصفوفة التنفيذ الذكي]
14. **مصفوفة الدخول المثلى**: تتطلب تلاقي 4 عناصر: OB + FVG + Sweep + Time Fib Level.

15. **النمذجة الزمنية الكمية**: حساب الوقت المطلوب للوصول لكل هدف باستخدام معادلة: (المسافة ÷ متوسط السرعة التاريخية) × عامل التصحيح.

16. **مفتاح القتل الذكي**: تحديد 3 نقاط إلغاء: فنية + زمنية + حجمية.

17. **تحليل التشفير للذيول**: دراسة التيول باستخدام نظرية المعلومات لتحديد نية المؤسسات.

[قسم إدارة المخاطر المتطورة]
18. **مصفوفة المخاطر الكمية**:
    - نسبة العائد/المخاطرة: لا تقل عن 1:3
    - أقصى مخاطرة: 2% من رأس المال
    - تقييم احتمالية النجاح: بناءً على 7 عوامل

19. **نظام الحماية المتدرج**:
    - **Stop Loss الأساسي**: خلف أقوى منطقة دعم/مقاومة
    - **Stop Loss الديناميكي**: يتحرك مع السعر بعد تحقيق TP1
    - **Stop Loss الزمني**: إذا لم يتحرك السعر لصالحك خلال X شموع
    - **SL النفسي**: مستوى يكون قبل SL الأساسي بـ 5-10 نقاط كمنطقة مراقبة

20. **تحليل السياق الاقتصادي المصغر**:
    - تأثير الأخبار على المدى القريب
    - أوقات التداول النشطة
    - ساعات السيولة العالية

المعطيات التقنية:
- إطار الشمعة : (Timeframe): {candle}
- وقت التداول المحدد : {time_for_prompt}

قدم التحليل باللغة العربية وبالتنسيق التالي حصراً (بدون تكرار للمعلومات):

📊 **التحليل الفني**:
- **البصمة الزمنية**: (داخل/خارج منطقة القتل السعري - Kill Zone)
- **حالة الهيكل**: (صاعد/هابط) + (مرحلة وايكوف الحالية)
- **خريطة السيولة**: (أقرب فخ سيولة Inducement + مناطق السيولة المستهدفة)
- **الفجوات السعرية (FVG)**: (المناطق التي سيعود السعر لتغطيتها)

🎯 **الإشارة التنفيذية**:
- **السعر الحالي**: [اذكر السعر الدقيق الذي تراه الآن للتأكد من دقة القراءة]
- **القرار الفني**: (شراء 🟢 / بيع 🔴)
- **قوة الإشارة 🔰**: (🔥 عالية - تلاقي 3 مدارس / ⚡ متوسطة / ❄️ ضعيفة)
- **نقطة الدخول (Entry)**: [ السعر الدقيق بناءً على الـ Order Block ]
- **الأهداف الربحية (TPs)**:
    - 🎯 **TP1**: [سحب أول سيولة داخلية],[احتمالية الوصول]
    - 🎯 **TP2**: [الهدف الرئيسي - منطقة عرض/طلب قوية],[احتمالية الوصول]
    - 🎯 TP3: [استهداف السيولة الخارجية (Major SSL/BSL) أو سد فجوة سعرية على فريم أكبر],[احتمالية الوصول]
- **وقف الخسارة (SL)**: [السعر مع 3 طبقات حماية]
- **المدة المتوقعة 🕧**: [عدد الشموع للوصول للهدف TP1 قم بتحويلها الى دقائق بالضبط كل شمعة 1 دقيقة]

⚠️ **إدارة المخاطر**:
- **مستوى الثقة**: [% مع ذكر عدد التاكيدات]
- **نقطة الإلغاء**: [السعر الذي يفسد التحليل]
        """
        
        payload = {
            "model": GROQ_MODEL,
            "messages": [
                {
                    "role": "user", 
                    "content": [
                        {"type": "text", "text": prompt},
                        {
                            "type": "image_url", 
                            "image_url": {
                                "url": f"data:image/jpeg;base64,{base64_img}"
                            }
                        }
                    ]
                }
            ],
            "max_tokens": 1500,
            "temperature": 0.3
        }
        
        headers = {
            "Authorization": f"Bearer {GROQ_KEY}",
            "Content-Type": "application/json"
        }
        
        response = requests.post(GROQ_URL, headers=headers, json=payload, timeout=60)
        
        if response.status_code == 200:
            result = response.json()['choices'][0]['message']['content'].strip()
            
            # ✅ تنظيف النص من التكرار
            result = clean_repeated_text(result)
            
            # ✅ إزالة أي تكرار محتمل
            # إزالة "### تحليل الشارت المرفق" إذا كانت موجودة
            if "### تحليل الشارت المرفق" in result:
                parts = result.split("### تحليل الشارت المرفق")
                if len(parts) > 1:
                    result = parts[1].strip()
            
            # إزالة أي "نتائج الفحص الفني:" إذا كانت موجودة
            if "نتائج الفحص الفني:" in result:
                result = result.replace("نتائج الفحص الفني:", "📊 **التحليل الفني:**").strip()
            
            keyboard = [["📊 تحليل صورة"], ["⚙️ إعدادات التحليل"], ["📈 توصية"], ["الرجوع للقائمة الرئيسية"]]
            
            # تنسيق وقت الصفقة للعرض
            time_display = format_trade_time_for_prompt(trade_time)
            
            # ✅ إعداد النص النهائي بدون تكرار
            full_result = (
                f"✅ **تم التحليل بنجاح!**\n"
                f"📈 **نتائج تحليل الشارت:**\n"
                f"━━━━━━━━━━━━━━━━\n"
                f"{result}\n\n"
                f"📊 **الإعدادات المستخدمة:**\n"
                f"• سرعة الشموع: {candle}\n"
                f"• {time_display}\n\n"
                f"━━━━━━━━━━━━━━━━\n"
                f"🤖 **Obeida Trading - نظام التحليل الفني**"
            )
            
            # تنظيف النهائي من التكرارات
            full_result = clean_repeated_text(full_result)
            
            # تقسيم النتيجة إذا كانت طويلة
            if len(full_result) > 4000:
                parts = split_message(full_result, max_length=4000)
                
                # إرسال الجزء الأول مع تعديل الرسالة المنتظرة
                await wait_msg.edit_text(
                    parts[0],
                    parse_mode="Markdown"
                )
                
                # إرسال الأجزاء المتبقية
                for part in parts[1:]:
                    await update.message.reply_text(part, parse_mode="Markdown")
            else:
                await wait_msg.edit_text(
                    full_result,
                    parse_mode="Markdown"
                )
            
            # إرسال الأزرار
            await update.message.reply_text(
                "📊 **اختر الإجراء التالي:**",
                reply_markup=ReplyKeyboardMarkup(keyboard, resize_keyboard=True, one_time_keyboard=False)
            )
        else:
            print(f"Groq Vision API Error: {response.status_code} - {response.text}")
            keyboard = [["الرجوع للقائمة الرئيسية"]]
            await wait_msg.edit_text(f"❌ **خطأ في إرسال الصورة:** {response.status_code}")
            
    except requests.exceptions.Timeout:
        await wait_msg.edit_text("⏱️ تجاوز الوقت المحدد إرسال الصورة. حاول مرة أخرى.")
    except Exception as e:
        print(f"خطأ في تحليل الصورة: {e}")
        keyboard = [["الرجوع للقائمة الرئيسية"]]
        await wait_msg.edit_text("❌ **حدث خطأ في إرسال الصورة.**\nيرجى التأكد من وضوح الصورة والمحاولة مرة أخرى.")
    finally:
        if os.path.exists(path):
            os.remove(path)
    
    return MAIN_MENU

# --- الدوال الأساسية ---
async def start(update: Update, context: ContextTypes.DEFAULT_TYPE):
    """بدء البوت"""
    keyboard = [
        ["⚙️ إعدادات التحليل", "📊 تحليل صورة"],
        ["💬 دردشة", "📈 توصية"]
    ]
    
    await update.message.reply_text(
        "🚀 **أهلاً بك في Obeida Trading **\n\n"
        "🤖 **المميزات الجديدة:**\n"
        "• تحليل فني متقدم للشارتات\n"
        "• 🆕 دردشة \n"
        "• 📈 نظام توصيات جاهزة\n"
        "• إعدادات تخصيص كاملة\n"
        "اختر أحد الخيارات:",
        reply_markup=ReplyKeyboardMarkup(keyboard, resize_keyboard=True, one_time_keyboard=False),
        parse_mode="Markdown"
    )
    return MAIN_MENU

async def handle_main_menu(update: Update, context: ContextTypes.DEFAULT_TYPE):
    """معالجة اختيارات القائمة الرئيسية"""
    user_message = update.message.text
    user_id = update.effective_user.id
    
    if user_message == "⚙️ إعدادات التحليل":
        keyboard = [CANDLE_SPEEDS[i:i+3] for i in range(0, len(CANDLE_SPEEDS), 3)]
        keyboard.append(["الرجوع للقائمة الرئيسية"])
        
        await update.message.reply_text(
            "⚙️ **إعدادات التحليل الفني**\n\n"
            "حدد سرعة الشموع للبدء:",
            reply_markup=ReplyKeyboardMarkup(keyboard, resize_keyboard=True, one_time_keyboard=False)
        )
        return SETTINGS_CANDLE
    
    elif user_message == "📊 تحليل صورة":
        candle, trade_time = get_user_setting(user_id)
        
        if not candle or not trade_time:
            keyboard = [["⚙️ إعدادات التحليل"], ["الرجوع للقائمة الرئيسية"]]
            await update.message.reply_text(
                "❌ **يجب ضبط الإعدادات أولاً**\n\n"
                "الرجاء ضبط سرعة الشموع ومدة الصفقة قبل التحليل.",
                reply_markup=ReplyKeyboardMarkup(keyboard, resize_keyboard=True, one_time_keyboard=False),
                parse_mode="Markdown"
            )
            return MAIN_MENU
        else:
            keyboard = [["الرجوع للقائمة الرئيسية"]]
            
            time_display = format_trade_time_for_prompt(trade_time)
            
            await update.message.reply_text(
                f"📊 **جاهز للتحليل**\n\n"
                f"الإعدادات الحالية:\n"
                f"• سرعة الشموع: {candle}\n"
                f"• {time_display}\n\n"
                f"أرسل صورة الرسم البياني (الشارت) الآن:",
                reply_markup=ReplyKeyboardMarkup(keyboard, resize_keyboard=True, one_time_keyboard=False),
                parse_mode="Markdown"
            )
            return ANALYZE_MODE
    
    elif user_message == "💬 دردشة":
        return await start_chat_mode(update, context)
    
    elif user_message == "📈 توصية":
        return await start_recommendation_mode(update, context)
    
    keyboard = [["⚙️ إعدادات التحليل", "📊 تحليل صورة"], ["💬 دردشة", "📈 توصية"]]
    await update.message.reply_text(
        "اختر أحد الخيارات من القائمة:",
        reply_markup=ReplyKeyboardMarkup(keyboard, resize_keyboard=True, one_time_keyboard=False)
    )
    return MAIN_MENU

async def handle_settings_candle(update: Update, context: ContextTypes.DEFAULT_TYPE):
    """معالجة اختيار سرعة الشموع"""
    user_message = update.message.text
    user_id = update.effective_user.id
    
    if user_message == "الرجوع للقائمة الرئيسية":
        keyboard = [["⚙️ إعدادات التحليل", "📊 تحليل صورة"], ["💬 دردشة", "📈 توصية"]]
        await update.message.reply_text(
            "🏠 العودة للقائمة الرئيسية",
            reply_markup=ReplyKeyboardMarkup(keyboard, resize_keyboard=True, one_time_keyboard=False)
        )
        return MAIN_MENU
    
    if user_message in CANDLE_SPEEDS:
        save_user_setting(user_id, "candle", user_message)
        
        keyboard = [TRADE_TIMES[i:i+2] for i in range(0, len(TRADE_TIMES), 2)]
        keyboard.append(["الرجوع للقائمة الرئيسية"])
        
        await update.message.reply_text(
            f"✅ **تم تعيين سرعة الشموع:** {user_message}\n\n"
            f"الآن حدد **مدة الصفقة** المتوقعة:\n\n"
            f"📊 **خيارات مدة الصفقة:**\n"
            f"• **قصير (1m-15m)**: تنفيذ سريع، مخاطر منخفضة\n"
            f"• **متوسط (4h-Daily)**: انتظار أيام، مخاطر متوسطة\n"
            f"• **طويل (Weekly-Monthly)**: استثمار طويل، مخاطر مرتفعة\n\n"
            f"اختر الإطار الزمني المناسب لاستراتيجيتك:",
            reply_markup=ReplyKeyboardMarkup(keyboard, resize_keyboard=True, one_time_keyboard=False),
            parse_mode="Markdown"
        )
        return SETTINGS_TIME
    
    await update.message.reply_text("❌ الرجاء اختيار سرعة شموع صحيحة.")
    return SETTINGS_CANDLE

async def handle_settings_time(update: Update, context: ContextTypes.DEFAULT_TYPE):
    """معالجة اختيار مدة الصفقة"""
    user_message = update.message.text
    user_id = update.effective_user.id
    
    if user_message == "الرجوع للقائمة الرئيسية":
        keyboard = [["⚙️ إعدادات التحليل", "📊 تحليل صورة"], ["💬 دردشة", "📈 توصية"]]
        await update.message.reply_text(
            "🏠 العودة للقائمة الرئيسية",
            reply_markup=ReplyKeyboardMarkup(keyboard, resize_keyboard=True, one_time_keyboard=False)
        )
        return MAIN_MENU
    
    if user_message in TRADE_TIMES:
        save_user_setting(user_id, "trade_time", user_message)
        
        keyboard = [["📊 تحليل صورة"], ["💬 دردشة"], ["📈 توصية"], ["الرجوع للقائمة الرئيسية"]]
        
        candle, _ = get_user_setting(user_id)
        
        await update.message.reply_text(
            f"🚀 **تم حفظ الإعدادات بنجاح!**\n\n"
            f"✅ سرعة الشموع: {candle}\n"
            f"✅ مدة الصفقة: {user_message}\n\n"
            f"يمكنك الآن تحليل صورة أو الدردشة:",
            reply_markup=ReplyKeyboardMarkup(keyboard, resize_keyboard=True, one_time_keyboard=False),
            parse_mode="Markdown"
        )
        return MAIN_MENU
    
    await update.message.reply_text("❌ الرجاء اختيار مدة صفقة صحيحة.")
    return SETTINGS_TIME

async def handle_analyze_mode(update: Update, context: ContextTypes.DEFAULT_TYPE):
    """معالجة وضع التحليل"""
    user_message = update.message.text
    user_id = update.effective_user.id
    
    if user_message == "الرجوع للقائمة الرئيسية":
        keyboard = [["⚙️ إعدادات التحليل", "📊 تحليل صورة"], ["💬 دردشة", "📈 توصية"]]
        await update.message.reply_text(
            "🏠 العودة للقائمة الرئيسية",
            reply_markup=ReplyKeyboardMarkup(keyboard, resize_keyboard=True, one_time_keyboard=False)
        )
        return MAIN_MENU
    
    await update.message.reply_text(
        "📤 **الرجاء إرسال صورة الشارت فقط**\nأو اضغط 'الرجوع للقائمة الرئيسية'",
        reply_markup=ReplyKeyboardMarkup([["الرجوع للقائمة الرئيسية"]], resize_keyboard=True, one_time_keyboard=False)
    )
    return ANALYZE_MODE

async def handle_photo_in_analyze_mode(update: Update, context: ContextTypes.DEFAULT_TYPE):
    """معالجة الصور في وضع التحليل"""
    return await handle_photo_analysis(update, context)

async def help_command(update: Update, context: ContextTypes.DEFAULT_TYPE):
    """أمر المساعدة"""
    help_text = """
    🤖 **أوامر البوت:**
    
    /start - بدء البوت والعودة للقائمة الرئيسية
    /help - عرض رسالة المساعدة
    
    ⚙️ **كيفية الاستخدام:**
    1. استخدم أزرار القائمة للتنقل
    2. أرسل صورة الشارت للتحليل
    3. اختر "دردشة" للاستفسارات النصية
    4. اختر "توصية" لتحليل العملات
    
    📈 **نظام التوصيات:**
    • تحليل فني للعملات والمؤشرات
    • أربعة أقسام رئيسية
    • توصيات مفصلة لكل عملة
    • تحليل سريع ومباشر
    
    ⏱️ **خيارات مدة الصفقة:**
    • **قصير (1m-15m)**: تنفيذ سريع، مخاطر منخفضة
    • **متوسط (4h-Daily)**: انتظار أيام، مخاطر متوسطة
    • **طويل (Weekly-Monthly)**: استثمار طويل، مخاطر مرتفعة
    
    📊 **مميزات البوت:**
    • تحليل فني للرسوم البيانية
    • دردشة ذكية مع الذكاء الاصطناعي
    • نظام توصيات العملات
    • حفظ إعداداتك الشخصية
    • واجهة سهلة بالأزرار
    """
    await update.message.reply_text(help_text, parse_mode="Markdown")

async def cancel(update: Update, context: ContextTypes.DEFAULT_TYPE):
    """إلغاء المحادثة"""
    await update.message.reply_text(
        "تم الإلغاء. اكتب /start للبدء من جديد.",
        reply_markup=ReplyKeyboardRemove()
    )
    return ConversationHandler.END

# --- الحل النهائي ---
def run_flask_server():
    """تشغيل Flask server"""
    port = int(os.environ.get('PORT', 8080))
    app.run(host='0.0.0.0', port=port, debug=False, use_reloader=False)

def run_telegram_bot():
    """تشغيل Telegram bot"""
    print("🤖 Starting Telegram Bot...")
    
    # تهيئة قاعدة البيانات
    init_db()
    
    # إنشاء تطبيق Telegram
    application = Application.builder().token(TOKEN).build()
    
    # معالج المحادثة
    conv_handler = ConversationHandler(
        entry_points=[CommandHandler('start', start)],
        states={
            MAIN_MENU: [
                MessageHandler(filters.TEXT & ~filters.COMMAND, handle_main_menu)
            ],
            SETTINGS_CANDLE: [
                MessageHandler(filters.TEXT & ~filters.COMMAND, handle_settings_candle)
            ],
            SETTINGS_TIME: [
                MessageHandler(filters.TEXT & ~filters.COMMAND, handle_settings_time)
            ],
            CHAT_MODE: [
                MessageHandler(filters.TEXT & ~filters.COMMAND, handle_chat_message)
            ],
            ANALYZE_MODE: [
                MessageHandler(filters.TEXT & ~filters.COMMAND, handle_analyze_mode),
                MessageHandler(filters.PHOTO, handle_photo_in_analyze_mode)
            ],
            RECOMMENDATION_MODE: [
                MessageHandler(filters.TEXT & ~filters.COMMAND, handle_recommendation_selection)
            ],
            CATEGORY_SELECTION: [
                MessageHandler(filters.TEXT & ~filters.COMMAND, handle_recommendation_selection)
            ],
        },
        fallbacks=[CommandHandler('start', start), CommandHandler('cancel', cancel)],
        allow_reentry=True
    )
    
    application.add_handler(conv_handler)
    application.add_handler(CommandHandler("help", help_command))
    application.add_handler(CommandHandler("cancel", cancel))
    
    # إضافة معالج للنصوص
    application.add_handler(MessageHandler(filters.TEXT & ~filters.COMMAND, handle_main_menu))
    
    print("✅ Telegram Bot initialized successfully")
    print("📡 Bot is now polling for updates...")
    
    # تشغيل البوت
    application.run_polling(allowed_updates=Update.ALL_TYPES, drop_pending_updates=True)

def main():
    """الدالة الرئيسية"""
    print("🚀 Starting Obeida Trading...")
    
    # تشغيل Flask في thread منفصل
    flask_thread = threading.Thread(target=run_flask_server, daemon=True)
    flask_thread.start()
    
    print(f"🌐 Flask server started on port {os.environ.get('PORT', 8080)}")
    
    # تشغيل Telegram bot في thread الرئيسي
    run_telegram_bot()

if __name__ == "__main__":
    main()
